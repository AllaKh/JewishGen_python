# CLAUDE.md — уроки и правила работы над JewishGen_python

Читай этот файл в начале каждой сессии. Он содержит всё, что было выучено на практике.

---

## Проект

Python-скрапер для генеалогического поиска. Лончер (`app.py` → `gui/launcher.py`) с карточками баз.  
Интегрированные скраперы: **JewishGen**, **FamilySearch**, **MyHeritage**, **АИС Скарб**. Остальные базы — ссылкой в браузер.  
Стек: Playwright (async), PySide6 GUI, python-docx, openpyxl.

---

## Правила работы с git

- **Никогда не делай `git add -A` или `git add .`** — только конкретные файлы.
- **Перед коммитом** убедись что файл действительно содержит нужные изменения.
- **Revert — опасная операция**: `git revert HEAD` откатывает к последнему _закоммиченному_ состоянию, а не к тому что было в working copy до твоего коммита. Если до твоего коммита были незакоммиченные изменения пользователя — они войдут в твой коммит. Revert их сотрёт. В таком случае нужно делать `git revert HEAD` (revert of revert), чтобы вернуться к своему коммиту, а потом точечно откатить только свои правки.
- **Коммить только то, что просит пользователь.** Не коммить сам по себе.

---

## FamilySearch — алгоритм скрапера (строго соблюдать)

> **ОБНОВЛЕНО: поиск теперь URL-based, НЕ заполнение формы/попапа** (`_fs_url` строит полный results-URL со всеми `q.*`/`f.*` и `page.goto`). Пункты 1–2, 9–10 ниже (форма + Advanced-попап) — УСТАРЕЛИ, оставлены для истории. **`_fs_url` сверен ПАРАМ-В-ПАРАМ с живым URL пользователя** (тест: payload→URL == её URL, diff пуст). Схема:
> - **Имя**: `q.givenName/q.surname` (+`.exact=on`). Basic Search в GUI = ТОЛЬКО имя/фамилия (NAME), как на сайте.
> - **Life Events — 5 штук, ВКЛЮЧАЯ Birth** (у каждого СВОЁ место!): `q.<ev>Place` (+`.exact=on`) и `q.<ev>Date.from/.to` (+`.exact=on`). Префиксы: `birthLikeDate/birthLikePlace`, `marriageLikeDate/marriageLikePlace`, `deathLikeDate/deathLikePlace`, `residenceDate/residencePlace`, `anyDate/anyPlace`. **Диапазон**: N=0 («this year»/Exact) → ТОЛЬКО `.from=year` (без `.to`!); N>0 → `.from=year-N&.to=year+N`. **`.exact=on` у даты — НЕЗАВИСИМЫЙ чекбокс «Exact +/−»**, НЕ выводится из N (у birth «this year» N=0 БЕЗ `.exact`; у death ±2 С `.exact`). Место birth было ПОТЕРЯНО (basic Birth Year без места) — теперь Birth = полноценный life-event с местом.
> - **Family ×3 — ИНДЕКСИРОВАННЫЕ параметры, НЕ повторяющиеся**: 1-й = базовый `q.spouseGivenName`, 2-й = `q.spouseGivenName.1`, 3-й = `q.spouseGivenName.2`. Exact: `q.spouseGivenName.exact` / `.exact.1` / `.exact.2`. (Раньше я слал повторяющиеся `q.spouseGivenName=A&q.spouseGivenName=B` — НЕВЕРНО.) Роли: spouse/father/mother/other, по 3 каждой.
> - **Alternate Names (ANCESTOR INFO) ×3** — альт-имена САМОГО предка → индексированные на его же name-параметрах: `q.givenName.1/.2/.3` + `q.surname.1/.2/.3` (exact `q.givenName.exact.1` …). **Best-effort** — в её sample URL альт-имён не было, схему вывел по аналогии с family. Карточки First+Last+Exact+`✕`, до 3, ссылка `+ Alternate Name` гаснет после 3.
> - `q.recordCountry` + `q.recordSubcountry="Country,State"`, `q.batchNumber`, `q.filmNumber`, `q.isPrincipal=true`, `f.recordType=0..7`.
> - **`tab=records&results=60` уже в URL → НЕ кликать HR-таб** (он пересобирает URL и сбрасывает фильтры). После goto: только логин (для открытия записей) → собрать `tbody tr` → скрапить.
>
> **GUI зеркалит форму сайта**: Basic = имя/фамилия + Exact; Life Events = КАРТОЧКИ (Place + Exact слева, Year + выпадашка диапазона + «Exact +/−» справа), Birth включён; Family Members = карточки с **двумя** полями (First+Last) + по Exact на каждом + `✕`, до 3 каждой роли (ссылка `+ Spouse` ГАСНЕТ серым после 3 — стиль `advBtn:disabled`); Country/State дропдауны (`config/familysearch_locations.json`); Record Type чекбоксы; Batch/Film/Principal; «Show Exact Search» прячет/показывает ВСЕ галки Exact (имя+места+даты+семья). Сбор результатов / открытие записей / скачивание — без изменений (НЕ ломать).

1. Открыть домашнюю страницу → заполнить First Names, Last Names (+ Place, Birth Year) → SEARCH.
2. Нажать таб Historical Records `[data-testid="hr-tab"]`. URL → `?tab=records`.
3. **Логин НЕ нужен** для показа строк таблицы HR tab. Строки видны без логина.
4. Собрать строки таблицы `tbody tr` МОЖНО без логина. Если 0 строк — увеличивать таймауты ожидания (XHR грузится медленно), логин тут не поможет.
5. **ВХОДИТЬ ОДИН РАЗ ДО открытия записей** — `_sign_in_if_needed` (кнопка nav `[data-testid="no-loggedin-sign-in-button"]`) сразу после HR-таба. С персистентным профилем на 2-м прогоне кнопки нет → пропуск. **БАГ, который я ловил**: полагаться только на ленивый логин «при открытии первой записи» → запись №1 = страница «Sign In» (тайминг редиректа), и этот сломанный стейт каскадом валит Advanced Search (кнопка попапа не находится) → супруга не вводится → 12 однофамильцев вместо 1.
6. `_sign_in_if_needed` ставит `logged_in_ref[0]=True` ТОЛЬКО при реальном входе (ложное «уже залогинен» не должно блокировать вход `_scrape_page` на записи №1).
7. Резерв: если ранний вход не сработал — `_scrape_page` логинится на первой записи (`"login" in url` ИЛИ `"sign in" in title`). После — `logged_in_ref[0]=True`, повторный логин не делать.
8. Все записи открывать на **одной** странице (`page.goto(url)`), не в новых вкладках; после каждой — назад на `results_url`.
9. **Advanced Search — ДО скрапинга, на залогиненной странице результатов** (reload → HR tab → попап `[data-testid="advanced-search-form-button"]` (ждать 10с) → заполнить поля/супругу → Search → заново собрать). **НЕ «после первой записи»** — там стейт сломан логином, кнопка попапа таймаутит. Advanced сужает выдачу (супруга → 1 запись) ДО скрапинга, не скрапим 12 лишних.
10. После Advanced форма пересобирает URL — заново дописать exact имени/фамилии/места (`_apply_exact`).

---

## FamilySearch — скачивание JPG

- **`expect_download` должен оборачивать ВЕСЬ блок**: первый клик на download кнопку + выбор JPG Only + клик confirm. Иначе если первый клик сам инициирует загрузку — событие пропускается.
- После клика на картинку — ждать `button[aria-label*="Download" i]` до 8 секунд перед тем как пробовать fallback-селекторы. После логина viewer грузится медленнее.
- Записи с `view=index` в URL — индексные, без изображения документа. Не вызывать `_download_jpg`, только сохранить thumbnail.
- Если `_best_img(page)` вернул пустую строку — не вызывать `_download_jpg` вообще.
- Таймаут `expect_download` — 45 секунд.
- Fallback если timeout: ждать 15 секунд появления файла в папке Downloads.

---

## FamilySearch — Advanced Search

### Открытие попапа
```
[data-testid="advanced-search-form-button"]
```
Нажать, ждать 1.5 секунды.

### Кнопка Search внутри попапа
```
[data-testid="search-button"]
```
Это `type="submit"`. Использовать именно этот testid, не `button:has-text("Search")`.

### Семейные члены — точные data-testid (из HTML)

| Родственник | Кнопка раскрытия | Given Name field | Given Exact | Surname field | Surname Exact |
|-------------|-----------------|-----------------|-------------|--------------|---------------|
| Spouse | `spouse-fieldGroupButton` | `spouseGivenName0-field` | `q_spouseGivenName_exact` | `spouseSurname0-field` | `q_spouseSurname_exact` |
| Father | `father-fieldGroupButton` | `fatherGivenName0-field` | `q_fatherGivenName_exact` | `fatherSurname0-field` | `q_fatherSurname_exact` |
| Mother | `mother-fieldGroupButton` | `motherGivenName0-field` | `q_motherGivenName_exact` | `motherSurname0-field` | `q_motherSurname_exact` |
| Other | `otherPerson-fieldGroupButton` | `otherGivenName0-field` | `q_otherGivenName_exact` | `otherSurname0-field` | `q_otherSurname_exact` |

**Важно**: плейсхолдеры у полей семейных членов **пустые** (`placeholder=""`). Искать только по `data-testid`, никогда по плейсхолдеру.

### Чекбоксы Exact
Кликать `[data-testid="q_spouseGivenName_exact"]` и т.д. напрямую. DOM-обход не нужен — testid известны.

### Поля событий (Birth/Marriage/Death/Residence)
```
input[placeholder*="City, County, State, Province, or Country"]
input[placeholder="Year"]
```
Перед заполнением кликнуть соответствующий таб (BIRTH / MARRIAGE / etc).

---

## FamilySearch — логин

- Форма: `#userName`, `#password`, `#login`
- Заполнять через `page.fill()` с retry до 3 раз (React-форма может сбрасывать значение)
- Ждать 2 секунды после появления `#userName` перед заполнением
- После `#login` — ждать редирект на `familysearch.org` без `login` в URL (таймаут 30 сек)
- **Персистентный профиль `.fs_profile`** (`launch_persistent_context`, как у MyHeritage) — логин/куки хранятся между прогонами, входим ОДИН раз, не каждый запуск («запоминай куки»). Первый прогон с пустым профилем всё равно требует ОДНОГО входа (профиль скрапера ≠ обычный Chrome пользователя). Снимать `Singleton*`-локи перед запуском; закрывать лишние восстановленные вкладки. `browser = ctx`, закрытие — `ctx.close()`. Профиль в `.gitignore`.

---

## FamilySearch — точное совпадение и год (per-field exact)

- **В GUI у КАЖДОГО базового поля свой чекбокс «Exact»**: имя, фамилия, место, год — все передаются и учитываются. Payload: `exact={name,surname,place,year}`.
- **Год — диапазон ±2 по умолчанию** (`_year_range(year, span=2)`): «1897» → `q.birthLikeDate.from=1895&to=1899`, чтобы записи 1898 (переписи) тоже попадали. Чекбокс «Exact» у года → `span=0` (`from=to=year`). **Урок**: точный 1897 ВЫКИДЫВАЛ нужные переписи 1898 — пользователь злилась «что угодно, только не эти три записи». Диапазон лечит.
- **Exact для имени/фамилии/места — добавлять `.exact=on` к тому `q.*`-параметру, который FS САМ положил в URL результатов** (`_apply_exact`, читает реальные `q.givenName`/`q.surname`/`q.anyPlace`/`*Place*` из URL), НЕ выдумывать имена полей. Год-фильтр (`birthLikeDate`) из place-exact исключать.
- `_search`, `_ensure_year_in_url` принимают `exact`; год+exact дописываются в URL одним `goto`. `_ensure_year_in_url` зовётся после HR-таба и после Advanced reload (они сбрасывают URL).

## FamilySearch — слипшиеся родственники в результатах

- Ячейку результатов (`evts`, `rels`) читать через **`innerText`**, НЕ `text_content()` — иначе роль и имя склеиваются: «SpouseRebecca M Sanders». `innerText` держит переносы строк FS.
- Подстраховка `_split_rels` (на случай инлайн-рендера): вставляет `\n` перед каждой ролью и `: ` после («SpouseRebecca M Sanders» → «Spouse: Rebecca M Sanders», каждый родственник на своей строке). Роли — английские (FS у пользователя английский), для других языков best-effort.
- В Word значение пишется через `set_cell_lines` (`docx_util.py`) — разбивает по `\n` и после года-склейки.

---

## Что НЕЛЬЗЯ делать

- **Логин — ОДИН раз ДО открытия записей** (`_sign_in_if_needed` после HR-таба), НЕ полагаться только на ленивый логин при первой записи (он флакует → запись №1 = «Sign In» → ломается Advanced Search). Собирать строки `tbody tr` можно и без логина (но мы всё равно входим до скрапинга).
- **НЕ запускать Advanced Search «после первой записи»** — только ДО скрапинга, на залогиненной странице (иначе кнопка попапа таймаутит, супруга не вводится).
- **НЕ использовать `.first` для полей семейных членов** — после раскрытия секции Spouse появляются новые поля с такими же плейсхолдерами как у предка. `.first` заполнит поля предка, а не супруга. Использовать `data-testid`.
- **НЕ использовать `button:has-text("Search")` для кнопки в попапе Advanced Search** — это может совпасть с другими кнопками. Использовать `[data-testid="search-button"]`.
- **НЕ делать поспешных выводов** о причине ошибки. "Строк: 0" — скорее всего медленный XHR, а не проблема с логином.

---

## GUI (PySide6)

- Advanced Search панель оборачивается в `QScrollArea` — если контент не влезает на экран, появляется скролл.
- Максимальная высота scroll area = 55% высоты экрана.
- Family Members секция: каждое поле (First Name, Last Name) имеет свой чекбокс "Exact".
- Автосохранение всех полей в `.fs_autosave.json` при каждом изменении.
- `_fam_fields[key]` хранит `(ff, cb_f, lf, cb_l)` — два инпута и два чекбокса.

---

## JewishGen scraper (jewishgen_scraper.py)

Переименован из `scraper.py`. GUI-файл `gui/jewishgen.py` импортирует `jewishgen_scraper as scraper`.

### Скачивание изображений из результатов JewishGen

Анализируется **последний столбец** каждой строки результатов:

| Что в ячейке | Действие |
|---|---|
| Текст начинается с `FHL` (ссылка) | FamilySearch microfilm → открыть вьюер на нужном снимке, скачать |
| Число ≥ 100 000 (большое) | Archive ref — НЕ открывать, ссылка уже в таблице |
| `2423962/3` или `2423962 / 3` (дробь) | То же — НЕ открывать |
| Число < 100 000 в виде `[398]` или `398` | Кликнуть, скачать изображение |

Также проверяется **предпоследний столбец**: если там кликабельное слово `Image` → кликнуть, скачать.

### FamilySearch microfilm viewer — ПРАВИЛЬНЫЙ алгоритм

**Главный урок**: НЕ заполнять input-поле (кириллический селектор ненадёжен).  
Использовать URL-параметр `?i=N` (0-based индекс):
- Снимок 61 → `?i=60`
- Снимок 218 → `?i=217`

```python
base_url = fs_url.split("?")[0]
target_url = f"{base_url}?i={img_num - 1}"
```

**После навигации на `?i=N`:**
- Ждём до 30с появления `img[src]` count > 5 (признак загрузки сетки)
- Если логин → заходим → FamilySearch редиректит обратно на `?i=N` через `state=` параметр
- Если редирект потерял `?i=` → навигируемся вручную на `target_url`
- Thumbnail N уже подсвечен синим → кликаем его → скачиваем

**Браузер для FHL**: отдельный (`async with async_playwright()`) с теми же флагами что FamilySearch scraper:
```python
args=["--start-maximized", "--disable-blink-features=AutomationControlled"]
no_viewport=True, accept_downloads=True
```
Если использовать JewishGen-контекст → FamilySearch выдаёт "Access Denied / Error 15".

### Именование файлов и директорий

- Папки с фото: `{критерии_поиска}_{название_базы}/`
- Файлы фото: текст из первых 5 колонок строки результата
- Word-файлы: `{критерии_поиска}_{название_базы}.docx`
- Excel-файл: `{критерии_поиска}_jewishgen_results.xlsx`

Функции: `_query_prefix(rows)`, `_row_label(row)`.

---

## АИС Скарб (skarb_scraper.py + gui/skarb.py)

Сайт `https://archiveskarb.by/db/`. **Логина нет.**

> **GUI — английский** (`gui/skarb.py`: «AIS Skarb», все метки/кнопки/диалоги на англ.), но **поиск остаётся РУССКИМ** — пользователь вводит кириллические фамилии (плейсхолдер-пример `Шендерович/Шур/Левин` оставлен кириллицей), параметр `q` и логика скрапера НЕ тронуты. Только этот GUI на англ — другие GUI не трогать.

- **Параметр поиска: `q`** (НЕ `ФИО`!). HTML: `<input type="text" name="q">`. Поиск нескольких фамилий через ` или `.
- **Ссылки записей**: `a[href*="ELEMENT_ID"]` (формат `/db/?ELEMENT_ID=275117`). Собирать через JS — надёжнее, чем `query_selector_all`.
- **Пагинация**: `a.modern-page-next` (текст «След.»). Брать href через JS, ходить по всем страницам.
- **Имя персоны**: из текста ссылки в результатах поиска (`row["name"]`), НЕ из `<h1>` страницы записи — там «АИС Скарб» (шапка сайта).
- **Поля записи**: JS-экстрактор пар «Метка: значение» (dl/dt-dd, таблицы, `<b>метка</b> значение`, текстовые строки).
- **Word/Excel**: ссылка-источник в КОНЦЕ записи; в Excel колонки `#, Имя, <поля>, Фото, URL`.

---

## MyHeritage (myheritage_scraper.py + gui/myheritage.py) — самый сложный

### «Ограничить поиск по категории» — дерево фильтров + краулер (как у Ancestry)
- **GUI**: сворачиваемая по стрелке секция (`_cat_btn` ▶/▼ → `_cat_scroll`) с РЕКУРСИВНЫМ деревом категорий (`_cat_node`: чекбокс + стрелка у узлов с детьми, дети строятся лениво). Грузится из `config/myheritage_categories.json` (метки САЙТА = русские для её аккаунта). `_category_filters()` → отмеченные метки («Все коллекции» = без фильтра, исключается). Заменил старый одиночный `f_category` комбо.
- **КРИТИЧНО — фасет «Narrow down by category» = кликабельные `<div class="name">` БЕЗ `href` (ПОДТВЕРЖДЕНО пользователем, она дала все уровни).** URL-DFS как Ancestry НЕВОЗМОЖЕН (нет href). НО клик по `.name` **НАВИГИРУЕТ** (страница перегружается, ФИЛЬТРУЕТ без нового поиска → антибот не злится) на URL с кодом:
  - L1 = `/research/category-2000/…?formId=bmd&qname=Name ln.Smith lnmo.3` (Реестры рождения, браков и смерти)
  - L2 = `/research/category-2010/…?formId=birth&…` (Записей о рождении)
  - L3 = `/research/collection-10442/…?formId=EWCRI_Births&…` (Англия и Уэльс… 1837-2005) — **ЛИСТ, «больше уровней нет!»**
  - Значит: `category-<код>` → под-категория (рекурсия), `collection-<код>` → коллекция-ЛИСТ (не рекурсить).
- **Краулер `myheritage_filter_crawler.py`** (`_READ_NAMES_JS`/`read_names`/`_click_name`/`_crawl_names`): ОДИН bootstrap-поиск «Smith» → читает `<div class="name">` (метит `data-pw-name=idx`) → **РЕАЛЬНЫМ кликом** по idx-му → ждёт навигацию → из `page.url` берёт `category-(\d+)`=рекурсия / `collection-(\d+)`=лист → `go_back` к соседям. dedup по коду И по нормализованной метке. `n0==0` (нет `a[href*=/category-]`) → идём этим клик-путём, иначе старый URL-`visit`. **Запускать ТОЛЬКО самой пользователю** (`python myheritage_filter_crawler.py`): из фонового процесса агента `launch_persistent_context(.mh_profile)` падает `TargetClosedError`. Бот-челлендж детектить и СТОП.
- **`config/myheritage_categories.json` СОБРАН ИЗ ЕЁ `results/MH.docx` скриптом `build_mh_categories.py`, НЕ краулером.** Парсинг docx: **жёлтая заливка** = маркеры «Первый/Второй/Третий уровень:» + примеры с тегом `<div class="name">…</div>`; **ярко-зелёная (BRIGHT_GREEN)** = нетегированные имена коллекций (берём весь текст строки). **ПРАВИЛА УРОВНЯ (выстрадал, переделывал 3 раза):**
  - Имя сразу после «Первый уровень:» = L1.
  - **State-machine:** «Второй уровень:» → дальше имена L2; «Третий уровень:» → дальше имена L3.
  - **Имя, за которым СРАЗУ идёт «Третий уровень:», = L2-заголовок.** Сиблинг-L2 (напр. «Записей о рождении», «Смерть…», «Бракосочетание…» под «Реестры…») часто БЕЗ своего «Второй уровень:» — только «Третий уровень:» ПОСЛЕ. Без этого правила они валились в L3 под «Церковные реестры» (баг, на котором она орала).
  - Часть L1 (Газеты, Книги, Публичные отчёты) — **ПЛОСКИЙ список коллекций на 2-м уровне, L3 нет вообще.**
  - **Счётчики бывают ОТДЕЛЬНОЙ строкой** («10 000+», «3 689», «997») → строку из ОДНИХ цифр пропускать (`_NUMONLY`, без дефиса — чтобы диапазоны «1879–1934» выжили); `declean` режет хвостовой счётчик, но ХРАНИТ годы/«часть N».
  - **Баг падал раньше: узлы — ПУСТЫЕ dict `{}` (Python FALSY)** → `last[L2] or last[L1]` сваливался на L1; чинить `is not None`.
  - Итог: **14 L1, 1409 узлов, JUNK=0** (проверять: ни одного имени из чистых цифр/пустого; имена с годами целы). Имена РУССКИЕ (её сайт) — скрапер кликает по ним.
- **Английский display (`mh_add_english.py`) — поиск по СМЫСЛУ, НЕ по позиции!** `MH-English.docx` НЕ совпадает с русским по порядку, и **частично НЕ ПЕРЕСЕКАЕТСЯ** (проверено): в EN газеты сгруппированы регионально («Names & Stories in Newspapers from OldNews.com (Ohio, Indiana, Michigan)»), а в RU — по штатам («Газеты Michigan, 1817-2009») → таких в EN-docx ПРОСТО НЕТ (EN Газеты ≈27 vs RU ≈101). Та же коллекция бывает под разными L1. **Объединённый RU/EN-файл от ЧатГПТ — 0% верных пар** (склейка двух разнопорядковых списков: из 443 пар с годами совпало 0). Позиционный матч = мусор. **Решение:** русское дерево ОСТАЁТСЯ; GUI двойная метка **«English / Русский»** (идентичность чекбокса = русское имя, поиск = язык сайта). Для каждого RU-имени матчер (`ranked`/`best`): кандидаты по общему ГОДУ (или токену для безгодовых) → счёт = **нечёткий Jaccard, годы ×2** (год — сильнейший язык-независимый сигнал); токены = транслит места + гео/генеалогический словарь + `_fuzov` (difflib ≥0.85 на дрейф «Оребро→orebro≈örebro»); **номера «часть 5» сохраняются** (часть5≠часть4), годы — в отдельный year-set. **Уже-английские RU (книги «BillionGraves», «The Union Regiments of Kentucky» — MH их не переводит) → en = само имя** (316 шт). 14 L1 — ручной `L1_EN`. Порог: score≥0.2 → берём EN-docx; иначе словарный перевод (рядом русский). **Итог: 81% (1149/1409) — точные названия из EN-docx, ~19% переведено** (то, чего в EN-экспорте нет). JSON `{ru:{en,children}}`. **Вооруженные силы — баг был в СТАРОМ коммите, в `b03aa0a`+ уже верно.** Иврит — так же (3-й экспорт). НЕ существует способа найти то, чего в EN-экспорте НЕТ — нужен полный EN-экспорт той же структуры.
- **АВТОРИТЕТНЫЙ источник EN — `results\МХ - сводная таблица.docx`** (ЧатГПТ выправил ВСЕ переводы; пользователь: «возьми эту сводную и добавь чего не хватает, НИЧЕГО НЕ УДАЛЯЙ»). Формат: таблица 2 колонки (RU-блок | EN-блок), внутри ячейки строки **выровнены построчно** (строка i RU ↔ строка i EN), счётчики идут в ОБЕИХ колонках на одной позиции, «русских названий нет» = у EN-коллекций нет RU (блок не выровнен → берём только строку 0 = L2-заголовок). Экстрактор: строки-числа и «нет»-заметки выкинуть; `len(ru)==len(en)` → zip, иначе только заголовок. **Все пары → `mh_en_overrides.json`** (merge, не удалять старое) + нормализованный/фаззи-мост (difflib ≥0.92) для мелких расхождений текста. Покрыло ~72% дерева ТОЧНО — мусор в больших блоках (смерти/браки/армия) заменён верными названиями. Остаток (нет в таблице) → словарь.
- **Словарь `ru_to_en` — ПЕРЕВОДИТЬ предлоги, не транслитерировать** (пользователь орала «Death v Georgia», «Marriage v India», «Court dela v India»): `PREP` = {в/во→in, от→from, для→for, по→by, о/об/на/с/у→DROP, и/а→&}; `смерти→Deaths`, `захоронения→Burials`, `окружн→county`, `призывник→conscripts`, `иммигрирующ→immigrating`, `немц→Germans`; +гео Utah/Rhode Island/New York/New Jersey/New Hampshire. Итог: «Смерти в Джорджии»→«Deaths in Georgia», «Браки в Индиане»→«Indiana Marriages», «…немцы»→«Germans Immigrating…».
- **`PHRASES` — ЦЕЛЫЕ словосочетания ДО пословного прохода** (иначе word salad «Карточки воинского учёта»→«map voinskogo ucheta»): «карточки воинского учёта»→«Military Registration Cards», «записи о смерти»→«Death Records», «реестр записей регистрации смерти»→«Death Registration Records», «приходские записи…»→«Parish … Records». EN-фраза уже латиница → пословный цикл её НЕ трогает. Плюс «округ X»→«X County» (порядок слов, post-regex), drop год/штат-филлеров, капитализация транслит-мест. **БАГ, который ловил: токенайзер `re.findall` без `A-Z`** (`[а-яёa-z…]`) СЪЕДАЛ заглавные первые буквы вставленной EN-фразы («Death Records»→«eath ecords») → класс обязан включать `A-Za-zА-ЯЁ`. Итог: видимого транслит-мусора ~10→1.
- **УНИКАЛЬНОЕ назначение EN — иначе один EN лепится на РАЗНЫЕ RU (баг «Romania, Iași, Civil Status Records» на 3 узла: Запись актов / Уругвай / Аляска).** Сначала резервируем EN у `OVR`/already-English/`L1_EN`, потом фаззи-узлы жадно по убыванию score берут СВОБОДНЫЙ EN (каждый EN один раз); ничего свободного ≥0.2 → словарный перевод. Итог `EN labels reused: 0`. **Пользователь-данные правятся через `config/mh_en_overrides.json`** (highest-priority): туда легли её 62 + правки явных ошибок (категория `Запись актов гражданского состояния`=«Civil Registrations», Уругвай/Аляска civil, «British Red Cross Volunteers» вместо «Ireland's Memorial», «U.S. … Mexican-American War» вместо «Italians Immigrating», Arizona/Indiana biographies). Нашла ошибку — добавляй пару в overrides, перезапускай `mh_add_english.py`.
- **Сортировка дерева — ПО АЛФАВИТУ (по EN-метке) на ВСЕХ уровнях, включая L1** (на сайте сортировка по числу совпадений, у нас их нет → алфавит). В `walk` сортируем `items` по `en.lower()`.
- **Дубль «ребёнок = имя родителя» убирать** (Бельгия «…Левенском университете» как L2 И как её же L3) — в `walk` пропускаем `ru == parent_ru`.
- **GUI: `&` в метке Qt съедает как мнемонику** → экранировать `&`→`&&` в тексте чекбокса (была «Schools  Universities» без амперсанда). Окно — top-level QScrollArea + старт ВСЕГДА свёрнут (adv/cat collapsed, `_load` НЕ восстанавливает adv_open/cat_open), `_fit` кап по экрану + on-screen. Даты — Day+Month+Year. «Clear all filters» — в секции фильтров (чистит категории+record-type), НЕ в advanced. Basic-имя — те же 4 опции, что сайт.
- **Глубокий фильтр = ПУТЬ, не лист** (`_category_paths` в GUI): чекнутый узел отдаёт цепочку L1→L2→L3 (дедуп в порядке дерева, `_cat_paths[cb]`), скрапер кликает каждый по очереди → ДРИЛЛ по фасету. Один клик листа на верхнем фасете его НЕ найдёт. Автосейв (`_category_filters`) хранит только листья, payload (`_category_paths`) — путь.
- **Скрапер**: `run_scraper(categories=[...])` + `_apply_category_filter(page,label,log)` — кликает категорию по `.name` на странице результатов (как `_apply_record_type_filter`), сужает server-side ПЕРЕД сбором. Сейчас сужает в ОДИН проход (все отмеченные применяются подряд — для ОДНОЙ ветки дрилл работает); отдельный документ-на-категорию (Ancestry-проходы) — доделать.

### Антибот: ТОЛЬКО персистентный профиль
- `launch_persistent_context(MH_PROFILE_DIR=".mh_profile")` — НЕ свежий контекст!
- Свежий контекст без cookies → MyHeritage флагует логин как suspicious и шлёт challenge-письмо «Confirm a login attempt».
- После первого успешного входа сессия запоминается, 2FA больше не спрашивает.
- Персистентный профиль **восстанавливает старые вкладки** при старте — закрывать все кроме одной.
- **НЕ хардкодить `user_agent`!** Был прибит `Chrome/124`, а Playwright обновил Chromium до 141+ → UA заявляет 124, а движок отдаёт API 141 → **классический бот-тэлл** («он видит, что ты скрипт»). Headed-Chromium и так отдаёт корректный совпадающий `Chrome/<ver>` (без «Headless»). Анти-детект: `--disable-blink-features=AutomationControlled` (уже делает `navigator.webdriver=false`) + init-script (webdriver/plugins/languages/chrome). **Диагностики гонять НЕ на `.mh_profile`** или с теми же флагами — headless без init-script показывает `webdriver=true` и пачкает репутацию профиля.
- **Куки-баннер — кликать ТОЛЬКО явный accept**: текст «Принять все»/«Accept all» по ВСЕМ фреймам, потом accept-специфичные селекторы (OneTrust). НЕ «первая кнопка в `[class*=cookie]`» — там бывает футер-ссылка/«Отклонить», клик по ней **ломает форму логина**.

### 2FA по почте (КЛЮЧЕВЫЕ уроки)
- MyHeritage шлёт **ДВА письма с РАЗНЫМИ кодами**:
  - **B** «Ваш код подтверждения для входа / verification code» — это код для поля «Введите верификационный код» (приоритет!)
  - **A** «Confirm a login attempt» (анти-фрод, flagged as suspicious) — запасной
  - Читать ОБА, пробовать B потом A.
- Чтение кода — **браузером, новой вкладкой** `mail.yandex.ru` (поле «Mail password» в GUI = пароль того же ящика, что логин MH):
  0. **СНАЧАЛА проверить, не залогинен ли уже** (`_mail_logged_in`): персистентный профиль почти всегда помнит сессию Яндекса → инбокс открывается сразу, **формы входа НЕТ**. Признак: НЕТ `#header-login-button` + есть кнопка «Написать»/строки писем/`#inbox` в URL. Если залогинен — **ВЕСЬ вход пропустить**. **БАГ, который я уже ловил**: гнать логин-цепочку при залогиненной почте → код кликает случайную ссылку `a:has-text("Войти")` (в рекламе/футере!), уходит с инбокса → поле логина не находит → вкладка ломается («Target page closed»), почта «не открывается».
  1. (если НЕ залогинен) `#header-login-button` («Войти»)
  2. `input[autocomplete="username"]` → email
  3. `button[data-testid="add-user-next"]` (Next)
  4. span «Log in with your password»
  5. `input[autocomplete="current-password"]` → пароль
  6. **Код берётся ИЗ СТРОКИ ПИСЬМА В СПИСКЕ (заголовок+превью), письмо НЕ открывается** — так оно и работало. `_scan_codes`.
  7. **САМОЕ ВАЖНОЕ — матчить ЦЕЛУЮ СТРОКУ письма, а не обрывки.** Код `253003` лежит в **превью**, а фраза «код подтверждения для входа» — в **теме**. С широкими селекторами `a, li, div` они попадают в РАЗНЫЕ элементы → ни один не совпадает и с фразой, и с кодом → **0**. **БАГ, который я уже допускал.** Брать контейнер всей строки: `[class*=MessageSnippet i], .mail-MessageSnippet, [data-test-id=message-snippet], li[data-id], a[href*="#message"]` — у него `textContent` = тема+превью. Fallback: письмо MyHeritage с `\b\d{6}\b` без явной фразы = код «B».
  8. Письма на **языке аккаунта** (у пользователя русские: «...на странице входа в MyHeritage: 253003»), не английские — регулярки держать многоязычными. NB: превью пишет «код подтверждения**, который**…» (без «для входа»!), поэтому фразу искать как `подтверждения для входа` по ВСЕЙ строке (тема даёт «для входа»), не привязываться к превью.
  9. `_open_and_read` (открыть письмо, читать из тела) — оставлен ТОЛЬКО как крайний резерв, если скан строки совсем не дал кода. В норме НЕ срабатывает.
- Перед вводом кода: `page.bring_to_front()` (вкладка почты была в фокусе, иначе ввод уходит «в никуда»).
- Кнопка подтверждения называется **«Отправить»** (НЕ «Авторизация» — это фоновая кнопка формы логина!). Искать внутри **модалки с полем кода** (поднять от поля кода к `[role=dialog]`/`.modal`/`form`, кликнуть кнопку внутри).
- **ОДИН** submit! Enter + клик одновременно → MyHeritage делает resend (новый код), старый аннулируется → пустые поля + таймер. Проверять результат до 18с, не отправлять повторно.
- **БАГ (co.il): автозакрыватель вкладок убивал почту.** `_on_new_page` (закрывает junk/facebook/blank-вкладки) закрывал и вкладку `mail.yandex.ru`, которую скрапер сам открывает для 2FA — через ~3с «не myheritage → закрыть». На `.com` (RU/EN/HE) куки уже есть → 2FA не нужен → баг невидим. На **`.co.il` куки НЕ распространяются** (другой домен) → нужен свежий вход + код → почта закрывалась → авторизация падала. **Фикс**: `_browser_read_yandex_code` ставит `ctx._mh_pause_autoclose=True` + `page._mh_protected=True` (снимает флаг в `finally`), а `_on_new_page` их уважает и не трогает вкладки `yandex`/`passport`/`mail`/`outlook`.

### После логина
- Сессия из профиля → редирект `/login` → главную семейного сайта. Если URL не `/login` → уже залогинены, форму логина НЕ искать.
- Страница выбора сайта `FP/select-site.php`: парсить `goToSiteClicked('ID','URL')`. Если уже на `family-sites/<slug>/<ID>` → достать ID из URL.
- URL поиска: `research?s=<ID>&lang=RU`.

### Язык запроса = язык сайта (КРИТИЧНО — многоязычный поиск, 7 языков)
**Архитектура (финальная):** GUI **всегда английский** (канонические значения, только display), а **скрапер сам переводит** значения в язык сайта при работе с MyHeritage. Язык берётся из **селектора `f_site`** (`SITE_PRESETS` → `lang=` в URL), а **НЕ из подписей GUI**.
- **Поддержано 7 языков**: `en, ru, he, fr, de, es, pt` (все пресеты `SITE_PRESETS`).
- `_site_lang(site)` → код языка (`.co.il`/Israel → `he`, остальные по подстроке). Кладётся в `params["lang"]`.
- `_UI_I18N` — словарь концепт → `{en,ru,he,fr,de,es,pt}` (search/more/father/mother/spouse/death/residence/military/immigration/keywords/gender/apply/male/female). Значение — строка ИЛИ список. **Добавить язык = дописать ключ**, `_ui_labels` авторасширяется (`_LANGS` + `list(d.keys())`).
- `_search_words(lang)` — слова кнопки «Поиск» на всех языках; используется в `_find_form_root` (btn_sels), `_fill_research_basic` (JS-аргумент), `submit_sels` в `_search`.
- `FILTER_MAP` (тип записи) и разделитель «расширения критериев» — расширены на 7 языков (best-effort для не-RU/EN).
- **Замечание про аккаунт**: язык интерфейса аккаунта MyHeritage может перебить `lang=` из URL после загрузки (напр. откатиться на русский). Это ОК: `_ui_labels` отдаёт **варианты всех языков**, поэтому какой бы язык ни оказался на странице — его метка в списке и матчится.
- `_ui_labels(concept, lang)` → список всех языковых вариантов, **язык сайта первым**. Передавать его в `_open_pill` и `get_by_text`: на странице существует только вариант загруженного сайта, а `verify_auto_id` (data-automations) подтверждает, что открылся нужный попап. Так один код работает на RU/EN/HE.
- **Поля и кнопки внутри попапов — по `data-automations`** (язык-независимо): `father_first_name_field`, `*_filter_apply_button` и т.д. Поэтому **неверное ивритское слово не сломает ввод** — пострадает только поиск пилла по тексту.
- Уже трёхъязычны: `FILTER_MAP` (тип записи), кнопка «Поиск/Search/חיפוש», `_fill_research_basic` (поля по `data-automations`/порядку), `_apply`, `_more_open`, разделитель «расширения критериев» (+ `הרחב`/`קריטריון`).
- `_RT_MAP` **многоязычный** (принимает и русские, и английские метки `record_type`).
- **ОШИБКА, которую я допустил раньше**: перевёл `RECORD_TYPE_OPTIONS`/`CATEGORY_OPTIONS` (данные!) на английский, но матчеры в скрапере искали русский текст на русском сайте → рассинхрон → **0**. Лечится тем, что **скрапер переводит сам** под `params["lang"]`, а не полагается на язык GUI.
- **`category` в поиске НЕ участвует** (только при чтении записи) — её перевод косметический.
- **Иврит — best-effort** (ивритские подписи MyHeritage я не сверял с живым сайтом). Если на HE-сайте пилл не открывается — уточнить точный текст подписи и поправить `_UI_I18N`.

### GUI расширенного поиска — переделан «как на сайте» (gui/myheritage.py)
- **6 ДАТ жизненных событий** (Birth, Marriage, Death, Military, Immigration, Any), каждая = виджет `_DateField`: **Year** + ▾-дропдаун («Match year exactly» чекбокс + радио This year/±1/±2/±5/±10/±20) + **Place** + ▾-дропдаун («Place must match»). Дропдаун = `QToolButton`+`QMenu` с `QWidgetAction`-обёрнутыми чекбоксами/радио (`_menu_dropdown`).
- **5 РОДСТВЕННИКОВ** (Father, Mother, Spouse, Child, Sibling — Child/Sibling раньше НЕ было), каждый = `_NameField`: First name + ▾(«Match name exactly»/«Spelling variations»/«Match initials»/«Starts with specific letters») + Surname + ▾(«Match name exactly»).
- Внизу: **«Match all terms exactly»** (вместо старого «Exact match for all parameters») + кнопка **«Clear all filters»** (`_clear_filters` сбрасывает все даты/имена/поля).
- **Payload (`_payload`)**: birth/death/father/mother/spouse шлёт СТАРЫЕ ключи (скрапер работает как раньше) + год-флаги из BIRTH-даты (`_birth_year_params`→ year_exact/year_1..year_20, place_match) + НОВЫЕ ключи (marriage_*, military_year/place, immigration_year/place, any_*, child/_last, sibling/_last) + структуры `dates`/`relatives` (с per-field match-опциями). Новые ключи в сигнатуре `run_scraper` (дефолты) — приняты, но в форму MH вшиты **best-effort** (полное заполнение marriage/military/immigration/any/child/sibling в попапе «+More» — доделать; birth/death/father/mother/spouse работают). `_save`/`_load` хранят `dates`/`relatives` целиком.

### Форма поиска
- Форма может быть в **iframe** — искать во всех `page.frames`.
- Если селекторы инпутов не совпадают — **разметить от кнопки «Поиск»**: подняться к контейнеру формы, пометить текстовые инпуты `data-pw-rf=first/last/year/place` по `data-automations`/плейсхолдеру/порядку, печатать через `page.keyboard`.
- «+ Больше» (advanced) открывать ТОЛЬКО если задан advanced-параметр.

### Результаты
- Открываются в **НОВОМ табе** — опрашивать `ctx.pages` до 25с, искать новую myheritage-вкладку с `research` в URL.
- **Кол-во на страницу — реальный элемент `[data-automations="selector_header"]`** (span с текущим «20»), НЕ `selector_header_container` (его НЕТ → `_set_results_per_page` рано выходил → оставалось 20, пользователь «я вижу, что не меняешь на 50»). Открыть его → **ждать ~2с** (длинный список опций рисуется лениво) → кликнуть лист-элемент с точным текстом «50» → ждать ~2с (результаты перегружаются).
- **Пагинация — ХОДИТЬ ПО ВСЕМ страницам**: `a[data-automations="next_icon"]` (кнопка БЕЗ текста, только иконка; класс `pagination_item_active … next_icon`). Останавливаться ТОЛЬКО на разделителе «расширения критериев» (`res["stop"]`) ИЛИ когда страница не дала НОВЫХ записей — **НЕ** по «имена перестали фаззи-матчиться» (старый `new_rel==0` обрезал пагинацию на глубоких страницах — баг «не ходишь по всем страницам»). `max_pages=25`. Клик next_icon — Playwright, фолбэк **JS `.click()`** (скрытый/перекрытый дубль) + повтор клика на середине ожидания; проверять, что первая `showRecord`-ссылка сменилась.
- **Длинные списки MH рисуются ~2с** → задержки: фасет «Narrow down by category» — ~2с после отрисовки + клик «Show more / Показать ещё» (`_expand_category_facet`, ~2с после каждого раскрытия) ПЕРЕД выбором категории; селектор «на странице» — ~2с при открытии и после выбора (см. выше).
- **Ссылки записей**: `a[href*="showRecord"]`, имя + % из карточки.
- **Разделитель «расширения критериев»**: собирать только записи **ДО** него (после — нерелевантные). Искать **наименьший** элемент, чей текст содержит и «расширени», и «критери» (фраза разбита по спанам; leaf-only поиск не находит). `stop=True` как только разделитель найден → пагинацию прекратить.
- **Точный поиск — через URL `exactSearch=1`**, НЕ через чекбокс (галка `aria-checked` не переключается ничем). Перестроить URL результатов: заменить/дописать `exactSearch=1`, `goto`, дождаться появления `a[href*=showRecord]` (SPA рисует после XHR — `networkidle` мало).
- **`exactSearch=1` строг по ВСЕМ полям сразу**: запрос с отцом+супругом+годом смерти легко даёт **0 точных совпадений** (у записей эти поля пустые/иначе записаны). Поэтому: запоминать `_noexact_url`, и если точный поиск собрал **0** → откатиться на обычные результаты (первая страница, самые релевантные сверху, кап 40). Так поиск никогда не вернёт 0, если записи есть.
- **Диагностика при 0 собранных**: дамп `_diag_results` (url, title, счётчики `showRecord`/`recordTitle`/`anyRecord`/`resultCards`/`formInputs`, признак разделителя/«нет результатов», sample ссылок). Живой сайт мне недоступен (Chrome блокирует myheritage) — без дампа причину 0 не угадать, не гадать.

### Запись (`_detail`)
- Имя — h1 ближайший к строке «В категории» (НЕ имя из меню аккаунта «Алла Хананшвили»!).
- Поля — по **белому списку** генеалогических меток (Имя/Рождение/Смерть/Отец/Мать/Муж/...), иначе в таблицу лезет мусор из боковой панели «больше записей» (Geni).
- Ссылка «Посмотреть полный профиль на этом сайте» — искать по всей странице.
- **Фото — через лупу**: кликнуть `div.main_record_image_zoom` (`.searchMainSprite.imageZoom`) → откроется попап с **полным** фото; взять самое большое `<img>` по площади; закрыть попап (кнопка/Escape). Качать через `page.request.get` (НЕ новой вкладкой — иначе «Target page closed» на ~16-й записи). Полное фото — на диск, в Word — маленькое (`Inches(2.2)`).
- **WEBP→PNG для Word**: MyHeritage отдаёт фото в **WEBP**, python-docx его не умеет (`add_picture` молча падает). Конвертировать через Pillow (`_to_png`) перед вставкой; JPEG/PNG/GIF/BMP пропускать как есть.
- Исторические записи: полное фото требует Omni — только превью (thumbnail в Word).

### Сохранение файлов — конфликт существующих (overwrite / append / skip)
- Перед записью `.docx`/`.xlsx` проверять `path.exists()`. Если есть — спросить пользователя через callback `ask_file_conflict(names) → "overwrite"/"append"/"skip"`.
- Callback проброшен как `ask_2fa_code`: `Worker.request_file = Signal(str)` → `_show_file_conflict_dialog` (QMessageBox: Overwrite / Append new results / Skip) → `provide_file_choice` ставит `threading.Event`. Скрапер блокируется до выбора (до 5 мин).
- `write_docx(path, recs, qlines, append=True)`: открыть существующий `Document(path)`, `add_page_break` + заголовок «➕ Appended N more…», дорисовать записи (рендер одной записи вынесен в `_docx_add_record`).
- `write_xlsx(path, recs, qlines, append=True)`: `load_workbook`, дописать строки с продолжением нумерации `#`, **новые поля → новые колонки** (по union заголовка). Нужен импорт `load_workbook`.
- Дефолт без callback / без конфликта = `overwrite` (старое поведение, ничего не ломается).

### Facebook-вкладки (бесконечно лезли)
- FB SDK с `connect.facebook.net` (НЕ только `facebook.com`). Блокировать route: `facebook.com`, `facebook.net`, `connect.facebook.net`, `fbcdn.net`.
- init-скрипт: удалять `a[href*=facebook]`, FB-iframe, `.fb-page/.fb-like` каждые 0.4с + переопределить `window.open` (return null для facebook).
- `ctx.on("page")`: закрывать новую вкладку, как только URL = facebook/blank-мусор; myheritage-вкладки (результаты) оставлять.

### Уроки «Alexander W Sanders» (документы, фильтры, Word) — НЕ регрессировать
Пользователь гоняла МНОГО раз. Аккаунт **платный** — пейволла НЕТ (мой headless ловил анти-бот «Купить подписку»/«Вы робот» — это НЕ её опыт; живой прогон грузит всё). Сайт мне напрямую недоступен (анти-бот), живой тест — только у неё.
- **Скан документа = НЕ `<img>`!** `img.document_viewer_image` отдаёт **низкоres-плитку** (3-18 КБ) ИЛИ иконку коллекции. Полный документ качается через СОБСТВЕННУЮ кнопку вьюера: **сперва открыть ПОЛНОЭКРАННЫЙ режим** (клик `.fullscreen_overlay_button` — она появляется при **hover** на скан), ТАМ кнопка `span.mediaItemDownload` (`onclick="documentViewer.downloadSource()"`). Обернуть в `expect_download` (как FamilySearch/gwar). Без входа в fullscreen `documentViewer` не инициализирован → кнопки нет → качается мусор. Фолбэк: пока в fullscreen — забрать URL самого большого `myheritageimages`-`<img>` (`_fs_img`); затем `document_viewer_image` с ожиданием декодирования; затем `info.photo`. Сохранять полный `doc_bytes` с реальным расширением (`.jpg/.pdf/...` по magic-bytes), в Word — превью.
- **Год рождения — АСИММЕТРИЧНО** (`_year_not_earlier`, правило пользователя): выкидывать тех, кто **РАНЬШЕ** искомого года рождения, оставлять **равных и ПОЗЖЕ** (не симметричный `_year_ok`!). Сравнивать с допуском ±5.
- **Год берётся из ОТКРЫТОЙ записи**, не только из карточки (`_detail_birth_year`): сперва поле «Рождение», иначе **год СУПРУГА как прокси поколения** (запись «Alexander женат на Cynthia, born 1848» = мужчина 1840-х, даже без поля «Рождение»). **Родителей НЕ брать** — они на поколение старше (у Александра-1897 родители 1858 → иначе ложно отсечётся). «(~1848)» из имени — `_name_year`.
- **«Уточнить по типу записи» — фильтр РЕЗУЛЬТАТОВ, его применяет САМ САЙТ** (server-side, мгновенно): на **странице результатов** кликнуть радио `[class*="styled_radio_label_paragraph"]` с текстом «Исторические записи»/«Семейные деревья» (`_apply_record_type_filter`), дождаться смены первой `showRecord`-ссылки. НЕ читать все 16 и резать самому (пользователь злилась: «сайт в миллион раз быстрее»). `_type_ok` (по `category`/url: `дерев|tree|geni`/`collection-1`) — только **подстраховка**, если радио не нашлось. **НЕ клик по ФОРМЕ**: `get_by_text("Исторические записи")` попадал в **навигационную ссылку категории** → `/category-5000/` → 0 результатов + двойное заполнение имени. `RECORD_TYPE_OPTIONS` в GUI — строчные («Historical records»), маппинг `_RT_MAP`.
- **Лимит длины значения поля был 600 → дропал «Родные брат/сестра»** (12 чел = 608 симв.) у одного дерева, у другого (короче) — нет. Поднял до 4000. Один `recordFieldValue` столько не наберёт легитимно.
- **Члены семьи — каждый на своей строке**: каждый человек в своём `<a>` → вставлять `\n` перед каждым (кроме первого), нормализатор с сохранением переносов (`normNL`), в Word — несколько абзацев в ячейке.
- **Word**: ОДИН пустой абзац между карточками (ставить ПЕРЕД карточкой при `i>1`, в конце НЕ ставить — иначе два); пустая строка ПЕРЕД «Домочадцы:», не после; домочадцы — таблицей.

---

## Jewish Pogroms (pogroms_scraper.py + gui/pogroms.py)

`jewishpogroms.info` — жертвы еврейских погромов в Российской империи (ПМВ/Гражданская). Карточка в лончере ПЕРЕД «Мемориалом», key `Pogromslogo`. **Полностью открытый WordPress, логина нет. Браузер НЕ нужен** (urllib, как wikisource): поиск = WP-AJAX POST `admin-ajax.php` `action=pogroms_search`, ответ — готовый HTML таблицы; карточки — обычные GET-страницы.

- **406 Not Acceptable на голый urllib** — слать браузерные заголовки (UA Chrome + Accept + Accept-Language), для AJAX ещё `X-Requested-With: XMLHttpRequest` и `Referer`.
- **Форма**: `family_name` (ТОЛЬКО латиница — сайт сам режет ввод `[A-Za-z *]`), чекбокс `soundslike=yes` (невыбранный чекбокс в POST НЕ передаётся — это и есть точное совпадение), 10 дропдаунов: `region, city, rec_type, incident, notes, found, register, case_number, archive, is_alive`. Их опции (значения из БД сайта, ~13к строк) лежат в **`config/pogroms_options.json`** — извлечены из инлайн-`<select>` главной; GUI грузит оттуда (комбо editable, `setMinimumContentsLength` — иначе длинные опции раздувают окно до 1500px).
- **Таблица результатов**: `<td class='pogroms-optional pogroms-<key>'>` с `<strong>метка</strong><span><a href='карточка'>значение</a></span>`; КАЖДАЯ ячейка строки ссылается на карточку ЭТОГО человека (`/pogroms_people/pogroms-post/?id=N`, у каждой строки свой id). Колонки: Region, Page, City, Record type, Family name, Name, Patronymic, Age, Incident, Is Alive, Notes, Found, Register, Case, Archive. Таблица → Word И Excel.
- **Карточка**: `h1.pogroms-person-title` (полное имя), `ul.pogroms-person-info` → `<li><span class="pogroms-person-info-type">Метка: </span> значение</li>` (Registration/Age/Incident/Is alive/Source), `div.pogroms-person-description`, `div.pogroms-person-notes`. Карточка → **ТОЛЬКО Word**. **Пустые метки ПИСАТЬ ВСЕГДА** (метка слева, пустая ячейка справа — требование пользователя). Фото `ul.pogroms-person-photos` — почти всегда пустой URL-каталог `uploads/pogroms/<id>/` (битая иконка) → качать best-effort, сохранять только реальные image-bytes (magic-check).
- Word: альбомная, шапка «Jewish Pogroms — результаты поиска», таблица результатов 15 колонок, затем по человеку: heading + 2-колоночная таблица всех меток + «Источник» ссылкой. Excel: `#, База (=Jewish Pogroms), <15 колонок>, URL («Открыть»)`. Файлы `pogroms_{Family}.docx/.xlsx`, конфликт overwrite/append/skip как в MH/FS.

## Memsearch (memsearch_scraper.py + gui/memsearch.py)

Мета-поиск по базам жертв советских репрессий (Memorial / GULAG.CZ), `memsearch.org`. **Это и есть бывшая карточка «Мемориал»** — НЕ создавать вторую (объединено в одну, key `Memlogo`). Логина нет. SPA. Сайт мне напрямую недоступен (Chrome MCP блокирует домен + сервер отдаёт 403) → структуру брал у пользователя из DevTools, Playwright на её машине сайт открывает.

### Архитектура language (как в MH)
- GUI **всегда английский** + селектор «Site / Language» (ru/en). Скрапер ходит на `memsearch.org/<lang>` и **сам переводит** табы/кнопки/поля/опции через `_I18N` (концепт→{ru,en}), матчит **оба** языка (`_t_all`). EN — best-effort (EN-DOM не сверял).

### Селекторы
- Поиск: `input.SearchInput` + кнопка со `span.RedButtonText` («ИСКАТЬ»/«Search»).
- Табы: `button.EntityTypeMenuItem` → `h6` текст (All types/People/Places/Objects/Documents). **Пагинация — ТОТ ЖЕ класс**, но `h6` = ЦИФРА (квадрат `h-12 w-12`). Различать: таб = текст, страница = цифра.
- Поля расширенного: `input[id="Фамилия"]`/`placeholder` (на EN — англ.). Дропдауны кастомные (div-триггер + кнопки-опции), не `<select>`.
- Имя карточки: `h4.Title`. **Коробка карточки = наибольший предок с РОВНО одним `h4.Title`** (иначе хватает табы+фильтры+все карточки; текст через `innerText`, не `textContent` — иначе пробелы склеиваются).

### Открытие записи — КЛИК, не href (КЛЮЧЕВОЙ урок)
- **У карточки НЕТ `<a href>`** на источник — она открывает внешний сайт **JS-кликом в новый таб**. НЕ пытаться достать href: мой regex выхватил `http://www.w3.org/2000/svg` из **xmlns SVG-иконки** → обе карточки → один URL → схлопнулись → открылась мусорная w3.org. **БАГ, который я уже допускал.**
- Правильно: `_tag_cards` помечает коробки `data-pw-card=i`; `_open_card` **кликает** карточку внутри `async with ctx.expect_page()` → ловит новый таб → скраплит **его** (`_extract_page`) → закрывает. URL источника = `np.url`.
- Внешние страницы разнородны (`ru.openlist.wiki`, `base.memo.ru` — **блокирует автоматизацию → `chrome-error`**, `jdoc.org.il`). Экстрактор generic: heading + пары «метка: значение» (dl/table/`<b>`/плоские строки) + самое большое фото.

### НЕ ПАДАТЬ НА ПУСТЫХ/БИТЫХ СТРАНИЦАХ (пользователь повторяла МНОГО раз)
- **`urlparse(u).netloc`, НЕ `.host`** — у Python нет `.host`; этот `AttributeError` убил весь прогон на `chrome-error`. Хелпер `_host()` ловит всё.
- **Каждая карточка в своём `try/except`** — одна битая/пустая НЕ валит прогон, берётся краткое из карточки, идём дальше.
- `chrome-error://`/`about:` → пропустить (одна попытка reload), записать краткое из карточки. Битые ссылки — норма, не повод падать.

### Вывод
- **Только Word** (Excel не нужен — записи штучные). Заголовки скромные: **жирный 12–14pt, НЕ `Heading 0/2`** (те дают гигантские буквы). Полная инфа из источника таблицей «Поле/Значение» + фото; если источник битый — краткое из карточки.
- Диалог конфликта файлов (overwrite/append/skip) — как в MH.

### GUI
- Поля расширенного поиска — `QStackedWidget` по табам; **схлопывать стек под текущую страницу** (`_shrink_stack`: не-текущим `QSizePolicy.Ignored` + `adjustSize`), иначе пустая полоса под мелкими табами.

---

## Память народа (pamyat_scraper.py + gui/pamyat.py)

`pamyat-naroda.ru` — записи об участниках ВОВ. GUI английский + селектор языка (ru/en). Логина нет. Сайт мне недоступен (Chrome/403) → DOM брал у пользователя. Это бывшая карточка-ссылка «Память народа» (key `Pamatlogo`).

### Поиск
- Сразу на **`?adv_search=y`**, поля по **`id`** (`#last_name`, `#first_name`, `#middle_name`, `#date_birth_from`, `#place_birth`, `#rank`, `#poslednee_mesto_sluzhbi` …) — **id язык-независимы**, работают на ru и en. Кнопка submit `input[value="Уточнить и найти"]`.
- Чекбоксы `#use_main_string` (вкл — игнорить верхнюю строку) и `#use_person` (вкл — группировать документы человека). Дропдауны `#army_unit_rod`/`#award_ids` — `select_option(value=…)` (значения язык-независимы).

### Нечёткое сравнение ФИО (КЛЮЧЕВОЕ — пользователь гоняла МНОГО раз)
- **Фамилия** — отличие ~1-2 буквы (`_sim ≥ 0.7`): Рогинский/Рагинский/**Рочинский**/Роминский/Родинский.
- **Имя** (`_first_match`) — Лев/**Лейб**/**Л.** (инициал ИЛИ первые 2 буквы ИЛИ `_sim ≥ 0.5`).
- **Отчество — ДИСКРИМИНАТОР по ОСНОВЕ** (`_patr_match`): «Михайлович» принимает «Михелевич», «**Мих.**», «М.» (одна основа `мих`), но **отвергает** «Максимович»/«Захарович»/«Яковлевич» (то же `-ович`, другая основа). Логика: префикс-сокращение ИЛИ совпадение первых 3 букв ИЛИ `_sim ≥ 0.78`.
- **Год рождения — ТОЧНО**: если задан, выкинуть людей, у кого в сниппете другой год (1921 → без 1922/1925). Сниппет брать, поднимаясь от ссылки до блока с годом (а не `closest` — он цепляет сам линк).
- **Пагинация — `onclick="getPage(N)"`** (не ссылка!): звать `getPage(n)` в evaluate, ждать смены первой ссылки.

### Карточки документов (по каждому человеку — НЕСКОЛЬКО)
- **Точный селектор `.hero-card-docs-item`** (НЕ `[class*="hero-card-docs-item"]` — он ловит и `__name`, и `-archive-button`, и саму карточку → **85 фейковых карточек**, БАГ).
- **Поля грузятся ТОЛЬКО при КЛИКЕ на карточку** → кликать каждую + раскрывать `.hero-card-docs-item-archive-button`.
- **Парсить по ФИКСИРОВАННОМУ набору меток** (`_CARD_LABELS`: Дата рождения / Воинское звание / Воинская часть / Источник информации / Фонд / Опись / Дело / Номер ящика …), а не generic-двоеточием — иначе имя+тип прилипают к первой метке. **Тип** карточки = текст до первой **данных**-метки (`_DATA_LABEL_RE`, без архивных), иначе «Картотека ранений» лезет в поля как «Картотека: ранений».
- **Сводка** (таб «Сводная информация») — метки БЕЗ двоеточия (две колонки) → резать по известным `_SUMMARY_LABELS`.
- Табы: `span.heroes_card__tabs-item-title` («Сводная информация» / «Боевой путь» — **грузится медленно, ждать** / «Дополнительная информация»).

### Сохранение СКАНА документа (пользователь дала кнопку с ПЕРВОГО раза — я прошляпил)
- **НАЖАТЬ `#btnSaveLocalImage`** (aria «Сохранить изображение»), когда документ загрузился (`img[usemap]` готов — скан это **image-map**, `<area>` = кликабельные строки), и поймать скачивание (`expect_download`). По каждой карточке.
- **НЕ ТРОГАТЬ «Скачать документы»** (`#heroCardBookletForm`, class `hero-card-booklet`) — это генератор **альбома**, не нужен.
- **НЕ «оптимизировать» ожидания сканов — документы ДОРОЖЕ времени** («Лучше уж подвисай»). Я дважды пытался ускорить пустые карточки и оба раза ТЕРЯЛ реальные документы → пользователь велела откатить всё (git checkout 881ae6e). Что НЕ работает: (а) проверка кнопки `#btnSaveLocalImage` по `offsetParent !== null` — у реальных документов кнопка бывает «невидимой» по этому критерию → записи с 3-4 документами получали «документов нет»; (б) пропуск карточки без декодированного image-map — терял сканы; (в) удаление reload-retry — повторная загрузка СПАСАЕТ незагрузившиеся сканы.
- **ФИНАЛЬНАЯ схема кнопки (продиктована пользователем, НЕ менять)**: по каждой карточке — ждать кнопку `#btnSaveLocalImage` в DOM до **6с** (проверка СРАЗУ + каждые 0.5с, мгновенно ловит здоровые страницы); нет за 6с → **reload страницы** → вкладка «Документы» → клик ТОЙ ЖЕ карточки → всего **~2с** на отрисовку (**второго 6-секундного ожидания НЕТ** — «не появилась сразу, иди дальше»); опять нет → следующая карточка. Кнопка есть → ждать image-map 14×1с → жать сохранение В ЛЮБОМ СЛУЧАЕ (`expect_download` 15с + фолбэк `_find_scan`). Внешний проход «ничего не сохранилось → reload → второй полный проход» ОСТАВЛЕН.
- **Word: пустой абзац — только МЕЖДУ карточками документов** (при `j>1` перед карточкой), НЕ после последней — иначе лишний абзац перед «Источник».

### Word
- **Отдельная таблица на КАЖДУЮ карточку** (тип в заголовке) + шапка (сводка/боевой путь/доп.). Между карточками — **один** пустой абзац (НИКОГДА не два).
- **Чистить мусор сайта** (`_clean_block`): «О проекте … © Министерство обороны», «Урок Победы» — режутся из всех текстов.
- **«Источник» = кликабельное СЛОВО**, длинный URL с `?backurl=…` спрятан в href (очистить `re.sub(r"\?.*$","")`), в тексте НЕ показывать.
- **Ордена** — в Word мелко (`Inches(1.3)`), как файлы **НЕ сохранять** (только байты); искать `<img>` рядом с классом/текстом `award/орден/медаль/наград`, исключая портрет и баннер «Урок Победы» (детали — в «Уроках сессии» ниже).
- **Сканы** — `Inches(3.3)` (НЕ 5.5), сохранять в `images/`. `_SCAN_IMG_JS` берёт ТОЛЬКО `img[usemap]` (см. «Уроки сессии»).
- **Фото человека** — `a.hero-card-person-photo__item`: качать через браузер-навигацию, фолбэк — скриншот элемента; `mkdir` ДО записи (см. «Уроки сессии»).
- **PermissionError** (docx открыт в Word) → сохранить под именем с меткой времени, не падать.

---

## Памяти героев Великой войны (gwar_scraper.py + gui/gwar.py)

`gwar.mil.ru/heroes/` — участники Первой мировой (1914–1918), то же ведомство, что «Память народа», но **проще**: каждый результат = ОДИН документ (не группа карточек на человека). Сайт **только русский**, GUI английский, логина нет, key `Voinalogo` (лого `config/Voinalogo.png`). Сайт мне недоступен (Chrome/403) → DOM брал у пользователя.

### Поиск (`/heroes/`, поля по `id`/`name`, язык-независимы)
- `last_name`, `first_name`, `middle_name`, `birth_date`, `birth_place_gubernia`, `birth_place_uezd`, `birth_place_volost`, `birth_place`, `rank`, `military_unit_name`, `event_place`, `data_vibitiya`/`data_vibitiya_end` (даты события), `fund`, `inventory`, `file`. Поля кастомные (`with-unilabel`, `data-placeholder`) → заполнять кликом + `keyboard.type` (как pamyat).
- **«Событие»** — `<select id="event_id">` под виджетом `selectric` (~54 операции ПМВ), `select_option(value=…)`; в GUI словарь `EVENTS` (англ. → value). GUI зеркалит секции сайта: Basic data / Place / Sections / Additional parameters (вкл. Событие + даты) / Document storage — пользователь сверяет с сайтом, не пропускать поля!
- **`birth_date` с маской `date+month`**: голый год (1889) маска ломает → НЕ заполнять поле, фильтровать по году в результатах (`want_year` из `birth_date`). Полную дату (с точками) — заполнять.
- **«Разделы»** = чекбоксы-источники `award_tag/dead_tag/frc_tag/commander_tag/prs_tag` (по умолчанию ВСЕ вкл). Кликать `label[for="…"]` только если нужно снять.
- Кнопка: `input.button-search-big[value="Найти"]`. «Больше параметров поиска» — раскрыть, если свёрнуто.

### Результаты
- Ссылки записей: `a[href*="/heroes/chelovek"]` (детали `/heroes/chelovek_plen…`). Имя = текст ссылки, сниппет = ближайший контейнер (для года/места). Нечёткое ФИО — те же `_person_matches` (фамилия ~0.7, имя Герш/Гирш, отчество по основе).
- **Пагинация — через URL-параметр `page=N`** (results URL уже содержит `&page=1`): `_with_page(url, n)` + `goto`, ждать смены первой ссылки. (`<a data-page>` — JS, href пустой.)

### Запись (детальная)
- `h1` = имя; подпись (тип, «Картотека военнопленных») = первый непустой элемент после `h1`. Поля левой панели — `_parse_fields` по фикс-меткам `_GWAR_LABELS` (вкл. варианты «Должность/Звание» с переносом строки).
- **Скан документа**: многостраничный («Страница X из N» → `/из\s*(\d+)/`). По каждой странице: кнопка `#btnSaveImage` (`icon-save-dropdown`) → `expect_download` (обернуть клик иконки + клик пункта дропдауна, как в FS); фолбэк — самый большой `<img>` через `_img_bytes_via_goto` (CDN блокит plain). Перелистывать страницы документа `td.to-right-arrow`. Имена: `{имя}_лист{N}.jpg`, в Word `Inches(4.5)`.
- Word: таблица полей + сканы + «Источник» (URL без `?backurl`); конфликт файлов overwrite/append/skip; PermissionError → имя с меткой времени.

### Живой сайт — что выучено (gwar, проверено в браузере пользователя)
- **Сертификат**: у gwar российский корневой CA, Chromium не доверяет → `Page.goto` валится `ERR_CERT_AUTHORITY_INVALID`. Контекст создавать с **`ignore_https_errors=True`** (в браузере у пользователя сайт «Не защищено», но грузится).
- **ДУБЛИРОВАННЫЕ поля (мобильная + десктопная версия, ОДИН `id`/`name`)**: обычный `locator("#id").first.click()` попадал в скрытый мобильный дубль → TimeoutError, поле НЕ заполнялось. Заполнять через JS (`_FILL_JS`): ставить значение на **ВСЕ** элементы с этим `id`/`name` (native value-setter) + слать `input/change/keyup`, снять класс `empty-field`. Так же дублирован `<select id="event_id">` (под `selectric`) и ссылки пагинации.
- **Пагинация — клик по `<a data-page="N">`** (AJAX, `href` пустой), кликать ВИДИМЫЙ дубль через JS. **НЕ листать через URL `?page=N`** — gwar грузит результаты AJAX'ом, такой URL открывает ПУСТУЮ ФОРМУ (баг «возвращаешься к пустой форме»). Нет ссылки `data-page` → последняя/единственная страница, стоп.
- **Сворачиваемые секции**: тумблеры класс `more-sign … _closed` (архивная: `<span class="more-sign-arc more-sign_closed visible">Поиск по архивным реквизитам</span>` — текст НЕ «Показать архивные»!). Раскрывать: кликать видимые `_closed` (по классу + тексту «Больше параметров»/«архивным реквизитам»), в несколько проходов (2-я появляется после 1-й). Открытая меняет класс на `_opened` → повторно не схлопывается.
- **Скан**: кнопка-дропдаун `#btnSaveImage` в Playwright **download не даёт** → не висеть 15с, `expect_download` таймаут 5с, рабочий путь — фолбэк «самый большой `<img>`» через `_img_bytes_via_goto`. Паузы на листах короткие.
- **ФАСЕТЫ боковой панели — ДВА механизма, НЕ путать (пользователь давала data-facet-value, я тупил):**
  - **«Известные личности» (notable)** — В URL: `filters=famous_type[Военачальники]` (ПОДТВЕРЖДЕНО рабочим URL пользователя). Несколько → через запятую.
  - **«Награды»/«Потери» (awards/losses)** — URL-ключа НЕТ (мне неизвестен). Применять **РЕАЛЬНЫМ кликом Playwright по `[data-facet-value="…"]`** (`_click_facet`/`_apply_award_loss_clicks`) — именно те `<span class="field-check-box-name-value" data-facet-value="убит">` / `data-facet-value="Георгиевский крест  IV-й степени (Солдатский)">`, что давала пользователь. **JS `.click()` НЕ тогглит** кастомный чекбокс gwar (это был баг «Выбрано 0/10») — нужен НАСТОЯЩИЙ мышиный клик (`page.locator(...).click()`). Матч data-facet-value — **с нормализацией пробелов** (на сайте ДВОЙНЫЕ пробелы: «крест␣␣IV»; в конфиге одинарные → normalize). **5 сек ПЕРЕД каждым** кликом фасета («перед КАЖДЫМ фильтром 5 сек»), затем **ОДИН «Применить»** (все фильтры одним поиском, cb79c8d). GUI шлёт `awards/losses/notable` = `ru`-значения (= data-facet-value), `_fc_*.checked()`.
- **Галка «Exact match» УДАЛЕНА** (пользователь: «убери галку строгое соответствие»). Сравнение ФИО — только фаззи (`_person_matches`: фамилия `_sim≥0.7`, имя/отчество по основе). `exact` больше нет ни в GUI, ни в payload.
- **Свободный текст карточки (`desc`, Биография) — ЖЁСТКО чистить от мусора сайта** (`_DETAIL_JS` JUNK + `_CHROME_MARKERS`). Пользователь требовала «ВСЁ что в карточке → в Word», но `desc` (leaf-блоки ≥80 симв.) нагрёб ВЕСЬ хром: «Вы используете слишком старую версию браузера», «Уважаемые пользователи… доступ к личным архивам временно ограничен», «Новое в 2022-2023…», «В случае обнаружения технических проблем», «Персоналий на образе документа: 0», «Нет данных о точках боевых действий», «Боевой путь строится на основе координат…», и **полосу номеров страниц «123456…755»**. Фильтр: расширенный JUNK-regex + отброс leaf-блока, где цифр >40 и >60% длины (полоса страниц). Тест — гонять на этих самых строках (она их прислала из плохого `gwar_Иванов.docx`).
- **Поиск по ИМЕНИ+ОТЧЕСТВУ, не только фамилии** (`_apply_facets_url`): URL ОБЯЗАН нести `last_name`+`first_name`+`middle_name` — **кириллицей, как введено, БЕЗ `_translit`** (живой URL = `last_name=%D0%98%D0%B2%D0%B0%D0%BD…`=«Иванов», т.е. Cyrillic). Я раньше `q.pop("first_name"/"middle_name")` → URL-поиск становился surname-only → «Найдено 100 325 Ивановых» вместо «Иванов Григорий». Форма (`_do_search`) заполняет все три, но фасет-URL их выкидывал — больше НЕТ. Пустое поле — `pop` (чтобы не фильтровать пустотой).
- **GUI динамика (`_fit`): НЕ хардкодить `setMinimumWidth(820)` намертво** — на экране уже 820 окно вылезает за край → горизонтальный скролл/обрезка (запрет пользователя). В `_fit` кап: `setMinimumWidth(min(820, scr.width()-16))` (можно сжаться ниже 820 на узком экране). Колонки фасетов: `colw = max(130, min(scr.width()-16-48, 1232)//3)` — заполняют ширину на большом экране, сжимаются на низком, 3×colw+хром не вылезает. H-скролл уже `ScrollBarAlwaysOff`. `_toggle_facets`→`_fit` (окно: фасеты закрыты ≈820, открыты ≈1300 — динамически). На её ~1920 — без обрезки; на крошечном (800) контент клипается, но окно в экране и без h-скролла.
- **GUI зеркалит секции сайта** — не пропускать поля (пользователь сверяет): Событие (`event_id`, 54 операции), даты события (`data_vibitiya`/`_end`), архивные реквизиты.

---

## Яд Вашем (yadvashem_scraper.py + gui/yadvashem.py)

`collections.yadvashem.org` — Центральная база имён жертв Шоа. **Открытый сайт, логина нет.** GUI английский + селектор языка поиска. Сайт мне недоступен (Chrome MCP блокирует `yadvashem.org`, WebFetch отдаёт пустой SPA-шелл) → DOM/URL брал у пользователя. Key `Yadvashemlogo`.

### Поиск — через URL-параметры (НЕ форму)
- Схема: `/{lang}/names/search-results?page=1&s_<field>_search_<lang>=ЗНАЧ&t_<field>_search_<lang>=yvSynonym`.
  - `s_…` = значение, `t_…` = тип поиска (`yvSynonym`/`literal`/`phonetic`). Суффикс `_<lang>` у name/place-полей; у года — `s_year_birth_search` + `t_…=exactly|year2|year5` (БЕЗ языка).
  - Реальные имена полей в `PARAM_MAP` (last_name, first_name, maiden_name, place_birth, place_permanent, place_during_war, place_death, father_first_name, mother_first_name, mother_maiden_name, spouse_first_name, spouse_maiden_name, submitter_first_name, submitter_last_name). **НЕ выдумывать `gn_*`** — это была ошибка, давшая мусор/карту вместо документов.
- **7 языков пути**: en/he/ru/es/de/fr — из селектора GUI → в `/{lang}/` и суффикс параметров.
- `place_mode` (byfield/anyplace), `global_text` — отдельные параметры.

### Сайт ГЛЮЧИТ → ВСЕГДА рефреш перед чтением (пользователь требовала)
- YV часто отдаёт **мусор/пустое с первого захода**. Поэтому: страница результатов И КАЖДАЯ запись — `goto` → **`reload`** → читать. Если запись вернулась ПУСТОЙ (0 полей и 0 док.) — **reload ещё раз и повторить** (1 ретрай), лог `↻ пусто — обновляю ещё раз…`.

### Запись (детальная)
- **Имя — из `h1`** (НЕ из поля! «ДАТА»/«Хая» как имя — был баг).
- **Record Details = блоки «метка⏎значение» в ДВЕ СТРОКИ** (метка, на следующей строке значение), НЕ «метка: значение». Брать самый внутренний 2-строчный блок (`_DETAIL_JS`, `twoLines`).
- **Несколько документов на человека = табы Material** (`.mdc-tab`, `span.mdc-tab__text-label` = «Record 1»/«Record 2»/…). Каждый таб — ОТДЕЛЬНЫЙ документ-источник. Обходить ВСЕ табы (`_REC_TABS_JS`/`_REC_CLICK_JS`, клик через JS), у каждого свой скан + свои поля.
- **БАГ, который ловил НЕСКОЛЬКО раз: полоса табов лезет в поля.** Два таба «Record 1»/«Record 2» = 2-строчный блок → захватывается как фейковое поле `метка=Record 1, значение=Record 2`. Фильтровать `_REC_TAB_RE` (`^(record|запис[ьи]?)\s*\d*$`) в `_clean_fields` И в `_clean_text`. **Синтетический тест с хардкод-полями это НЕ ловит** — проверять на РЕАЛЬНОМ `.docx`: распаковать zip, искать «Record» в `word/document.xml` (строка может прятаться в ячейке таблицы!).

### Скан документа — LAZY (был баг «документов: 0»)
- Скан грузится **лениво**: `naturalWidth=0` пока не проскроллишь к нему; превью-миниатюра <200px. **НЕ гейтить по `naturalWidth≥200`** — иначе и ленивый скан, и миниатюра отсекаются → 0.
- Кандидаты брать широко: `currentSrc/src/data-src/data-original/srcset`, фоновые `background-image`, картинки во **фреймах** (same-origin); гейт 150px, **ленивые (unknown size) пропускать как большие**. Перед сбором — **пошаговый скролл всей страницы** (декодирует lazy). Байты валидировать `_looks_image` (magic) при скачивании → битый HTML не сохранится как `.jpg` (баг «ДАТА_1.jpg не открывается»).
- Карты Google исключать (`_BAD_IMG`). Качать через `_img_bytes_via_goto` (новая вкладка + referer).

### Мусор (чистить)
- `_JUNK` (соцсети, «база данных имён», «расширенный поиск», «очистить всё», `keyboard_arrow*`, карта/гугл/языки, …), `_ICON_RE` (Material-иконки). URL в `qlines` НЕ класть.

### Word
- **Каждый документ = СВОЯ таблица** (per-tab). Порядок внутри документа: сканы → его таблица; между документами ОДИН пустой абзац. **Никаких подписей «Record N»** (пользователь злилась МНОГО раз). Шапка: заголовок + `Запрос:` + `Найдено: N` (НЕ «Records»).
- Имена файлов скана: `{base}_rec{ti+1}_{n}` (мультитаб) / `{base}_{n}`. WEBP→PNG (`_to_png`) перед вставкой. Конфликт файлов overwrite/append/skip.

### GUI
- Английский, синяя тема. **Видны всегда**: язык поиска / Name / Date. **Расширенный поиск** (Place / Family members / Submitter / Global) — в **сворачиваемой кнопке-тумблере** `▶/▼ Advanced search` → раскрывает `QScrollArea` (max 50% экрана, свой скролл).
- **Одинаковая ширина ВСЕХ полей**: единый грид-конфиг `_cfg_grid` (метка `setColumnMinimumWidth(0,175)` | инпут `setColumnStretch(1,1)` | комбо 130px). Раньше поля «Место» были короче — лечится единым гридом.
- Высоту окна **капить экраном**: `_fit()` → `min(sizeHint, avail-48)`, иначе уезжает за низ.

---

## Вікіджерела (wikisource_scraper.py + gui/wikisource.py)

`uk.wikisource.org` — украинская Wikisource: архивные сканы еврейских записей (ревизские сказки, метрические книги, рекрутские списки…). Логина нет. Key `Vikijerelalogo`. **Заменил карточку «Великія описи»** (там нет документов).

### Сайт ДОСТУПЕН напрямую (в отличие от yadvashem/mil.ru)
- Wikimedia НЕ блокируется ни Chrome MCP, ни WebFetch, ни `urllib` с моей машины → DOM/структуру беру сам через **MediaWiki action API**. Это первый скрапер **без браузера** (urllib + API), Playwright не нужен.
- Namespaces uk.wikisource: **Архів = 116, Індекс = 252** (НЕ 104/106 — те «Сторінка»).

### Дерево «Архів:Єврейське містечко»
- Корень → **регионы = `== H2 ==` заголовки** корневой страницы (`_live_regions`, читается вживую). **14 регионов**, не 12: у некоторых (**Брацлавське воєводство**, **Переписи різних місцевостей**) НЕТ подстраницы — документы лежат прямо в секции корня (`_region_wikitext` → подстраница ИЛИ `_section_slice` корня). «Примітки» (сноски) исключить.
- В странице региона документы — **ДВА типа ссылок**: прямые `[[c:File:...|label]]` (≈317 на Волынь) И **индексные страницы** `[[Архів:CODE]]` (≈13, напр. `Архів:ЦДАВО/1854/1/136`). У индексной файл спрятан в шаблоне `{{Архіви/справа | link_commons = File:...}}` → `_index_to_file` (regex `link_commons\s*=\s*File:`). **Парсить ОБА** (иначе теряешь ~13 док/регион — БАГ, который я ловил).

### Скачивание — по md5-пути, без API-вызова
- Полный PDF: `upload.wikimedia.org/wikipedia/commons/{h0}/{h0h1}/{quote(fn)}`, где `h=md5(fn.replace(' ','_'))`. Проверено: совпадает с «Full resolution». Так обхожу весь ручной танец «Download all sizes → Full resolution / клик по обложке дважды → ждать рендер PDF».
- Превью (обложка JPG): imageinfo `iiurlwidth` (произвольная ширина рендерится по запросу; конструировать `page1-Npx-thumbnail` нельзя — 400 на не-разрешённой ширине).
- **Перемещённый файл** (404 без редиректа, напр. дублированный префикс `ДАЧгО ДАЧгО-...`): читать **move-log** `list=logevents&letype=move&letitle=File:OLD` → `params.target_title` = новое имя, качать его.

### HTTP 429 — Wikimedia режет массовые скачивания больших файлов (PDF ½–1.3 ГБ!)
- «Зависание» = пауза анти-429, не баг. Лечение: вежливый **мин. интервал между скачиваниями, растущий после каждого 429** (`_dl_gap`), **backoff с уважением `Retry-After`** (не сдаваться, а ждать и повторять), **resume** — если файл уже на диске (>10 КБ), пропускать (не качать заново 500 МБ).

### Перевод запроса (КРИТИЧНО — пользователь вводит RU/EN, сайт украинский)
- Место (повіт/місто) вводится по-русски или по-английски → матчить против украинских секций через **транслитерацию** (`_translit`: и/і/ї/й/ы/y→i, ц/ts→c, х/kh→h, …) + **стем-матч** (первые 6 букв, чтобы пройти склонения: «Сураж»→«Суражського», «Сосниця»→«Сосницького»). Проверено: `Surazh`=`Сураж`, `Sosnitsa`=`Сосниця`.
- Регионы/типы документов — словари `REGIONS`/`DOC_TYPE_TR` (uk→(en,ru)); ключ всегда украинский (для API), показ — двуязычный.

### Язык интерфейса (пользователь требовала — НЕ перепутать)
- **GUI английский** + названия регионов/типов **англ/рус** (английский первым). Сообщения/статус/лог — **английские**. Украинского НЕТ НИГДЕ (регион в статусе — `_region_en`).
- **Word-манифест — РУССКИЙ**: заголовок, `Запрос:`/`Режим:`/`Всего:`, колонки **№ / Название документа / Файл / Источник**. Ширины колонок: **№ и Источник — узкие, Название документа и Файл — широкие** (`tbl.autofit=False` + ширина на КАЖДОЙ ячейке построчно, иначе python-docx игнорит). Папка/имя манифеста — по `_region_ru` (русское имя региона). Названия самих документов — украинские (реальные архивные заголовки, НЕ переводить).
- **ОШИБКА, которую допустил**: перевёл ВЕСЬ GUI на русский — пользователь хотела GUI английским, а на русском только манифест. Откатил.

---

## Ancestry (ancestry_scraper.py + gui/ancestry.py)

`ancestry.com` — крупнейшая платная генеалогическая база. **Логин обязателен, анти-бот** (как FamilySearch/MyHeritage). Карточка в лончере key `Ancestry` (лого `config/Ancestry.png`), `action=open_window`. Построен **по образцу FamilySearch** (персистентный профиль, вход ОДИН раз до записей, per-field exact, один проход, Word+Excel с названием сайта, конфликт overwrite/append/skip, не падать). Сайт мне **недоступен напрямую** (Chrome-расширение блокирует домен `ancestry.com` → `Navigation to this domain is not allowed`; залогинена пользователь в Brave) → строил по её скринам/селекторам + знанию URL-схемы Ancestry; **результат-строки / детальная карточка / вьюер сверять по ЖИВОМУ прогону** (есть `_diag`-дамп при 0 результатов).

### Вход / профиль
- **Персистентный профиль `.ancestry_profile`** (`launch_persistent_context`, БЕЗ хардкода user_agent). Вход один раз, дальше помнит. В `.gitignore`.
- **Вход ОДИН раз ДО поиска**: открыть `/`, `_sign_in_if_needed` (залогиненность = НЕТ короткой ссылки «Log in/Sign in» в nav) → `/account/signin` → `#username`, `#password`, Sign in (`#signInBtn`). ПОТОМ поиск.

### Поиск — ТОЛЬКО через URL (домашняя форма ненадёжна)
- `#firstName/#lastName/#birthYear` на главной **НЕ находились** (форма меняется) → строю URL и `goto`, форму не заполняю.
- **URL** (сверено с целевым URL пользователя): `/search/?name=First+Middle_Last&birth=YEAR&birth_x=<N>-0-0&residence=PLACE&father/mother/spouse/sibling/child=First_Last&keyword=…&gender=m|f&race=…&count=50`.
  - **Имя = одно поле** `name=Имя+Отч_Фамилия` (пробел=`+`, имя↔фамилия через `_`). Exact имени → `name_x=1` (покрывает И имя И фамилию; отдельного given/surname-exact в URL нет → first-exact ИЛИ last-exact = один `name_x=1`).
  - **Год = диапазон `birth_x=<N>-0-0`**, N = ±лет (0 = точный год). Подтверждено: «+/-1 yr» → `birth_x=1-0-0`. В GUI — выпадашка как в MH (Exact/±1/±2/±5/±10), N передаётся `year_range`. БЕЗ диапазона записи 1898 (переписи) выпадают.
  - **`count=50`** обязательно (50 на страницу — релевантные все на 1-й странице).
  - **Родня — СПИСКИ** (неограниченно): `adv[rel]` = list of `{first,last,exact}`; в URL повторяющиеся `spouse=…&spouse=…`. GUI: кнопки «+ Add Father/Mother/Spouse/Sibling/Child», динамические строки с ✕.
  - **Галки Exact по умолчанию ВЫКЛ** (пользователь их НЕ ставила; её правильный URL без `name_x`/`spouse_x`).
- `_build_search_url` собирает строку ВРУЧНУЮ (literal `+`; `urlencode` сломал бы `+`→`%2B`).

### Сбор результатов — СЧИТАТЬ по ИМЕНИ ЧЕЛОВЕКА, не по названию коллекции (ГЛАВНЫЙ баг)
- **БАГ, который я допустил**: ссылка в строке результата = НАЗВАНИЕ КОЛЛЕКЦИИ («1930 United States Federal Census»), а ИМЯ человека («Alexander W Sanders») — в отдельном поле строки. Я скорил по тексту ссылки → релевантные записи получали 30%, мусор (где в тексте было имя) — 90%. Сохранились 2 нерелевантные, ни одной нужной.
- **Фикс** (`_COLLECT_JS`): для каждой record-ссылки подняться к КОНТЕЙНЕРУ строки (предок с метками `Name/Spouse/Birth/Residence`), взять значение ПОСЛЕ строки-метки «Name» как имя человека, дедуп по строке, выбрать лучший record-link (`discoveryui-content/view` › `family-tree/person` › `imageviewer`). Скорить `_match_name`.
- **`_match_name` (surname-aware)**: фамилия должна совпасть **≥0.94** (SequenceMatcher) — «Sanders»=1.0 ОК, но «Saunders»≈0.93 / «Sanderson»≈0.88 / «Snyder»≈0.6 ОТВЕРГАЮТСЯ (пользователь хочет именно эту фамилию). Имя — по инициалу/префиксу/sim≥0.7. «Alexander Wolf Sanders» (дерево) проходит (фамилия точная, Wolf≈инициал W). `MIN_MATCH=80`.

### Детальная карточка записи (`/search/collections/<db>/records/<rec>`)
- **Поля = строки «метка | значение» (`_FIELDS_JS`)**: класс-агностично — РОВНО два однострочных текстовых ребёнка (для `<tr>` — ровно 2 `td/th`), метка `<45`, значение `<200`, БЕЗ переносов. **ВАЖНО — не хватать таблицу целиком**: `<table>`=`thead`+`tbody` (2 ребёнка) хваталось как ОДНА склеенная пара «все заголовки|все данные» (баг «Line NumberNameResidence Age…C161John J Smith…» в Word) → пропускать элементы, чьи дети содержат `table/td/th/tr/li`.
- **Домочадцы (`household`)**: ТОЛЬКО таблица с «Household Members» (НЕ по слову «Relationship» — оно ловит широкую таблицу соседей), ячейки короткие (`<40`, иначе склейка), шапку пропускать.
- **«Others in Record» слипалось** («Anna M SmithJohn T SmithAnna M Smith»): значение = список ССЫЛОК на людей, `innerText` их клеит. Фикс в `_FIELDS_JS`: если у значения ≥2 дочерних `<a>` — собрать их тексты, **дедуп `Set`** (имя повторялось), выкинуть «More»/«See all», join `\n` (в Word `set_cell_lines` разложит построчно). Сплит ТОЛЬКО по `<a>` (не по `children` — иначе «1850»+«United States» разорвёт на 2 строки).
- **КРИТИЧНО — split по ссылкам ЧУТЬ НЕ УБИЛ ВСЕ поля** (регрессия, пользователь в ярости): у значений на детальной странице рядом с текстом есть **служебные ссылки Search/View/иконки**. Моя первая версия при `links≥2` БРАЛА значение ИЗ ССЫЛОК, ВЫКИДЫВАЯ текстовый узел → «Name» стало «Search», часть полей пустела. Фикс: значение ВСЕГДА `= norm(innerText)` (как было); сплит на строки **ТОЛЬКО когда ссылки реконструируют ВСЁ значение** (`links.join('').replace(/\s/g,'')===v.replace(/\s/g,'')` — т.е. значение целиком из ссылок, чистый список людей), иначе текст не трогать. Гарантия: поле НИКОГДА не пустеет, если `innerText` непустой. **Проверять на РЕАЛИСТИЧНОМ DOM** (значение = текст/ссылка + иконки Search/View с пустым `innerText`), а не на простом — простой тест баг НЕ ловит (с иконками `links<2` → ок; баг только когда ≥2 ссылки с текстом).
- **НИКОГДА НЕ ОБЪЕДИНЯТЬ карточки — ОДНА карточка на сайте = ОДНА запись в Word/Excel** (железное правило пользователя, повторяла в ярости). Я добавлял пост-дедуп `pass_recs` «по одинаковому title со совместимыми полями» (`_compat`/`_rich`) — он **СХЛОПЫВАЛ похожие-но-РАЗНЫЕ карточки** (в census-проходе «Подходящих: 29» → «Готово: 13» — потерял ПОЛОВИНУ записей!). **УДАЛЁН НАВСЕГДА.** Единственный допустимый дедуп — по **точному `r["url"]`** (та же карточка собрана дважды), он уже в сборе (`_COLLECT_JS` seenRow/seenHref + `seen` по url в пагинации). Похожие/одинаковые-title карточки с разными image-ref — это РАЗНЫЕ карточки → остаются РАЗНЫМИ записями. Если на сайте N карточек — в файле ровно N записей.
- **ГЛАВНЫЙ баг «в Word/Excel ничего, полей: 2»**: у census-записей ссылка ведёт во **вьюер изображения** (`/imageviewer/collections/<db>/images/...&pId=<rec>`), а у него карточки Detail в DOM НЕТ (только chrome вьюера → «Коллекция|View», Zoom in/out). Карточка живёт на **детальной странице записи** `/discoveryui-content/view/<pId>:<db>` (то же, что справа во вьюере по `iconCollapseRight`). Фикс `_detail_url`: если `pId`+`/collections/<db>/` и это imageviewer → строю detail-URL, открываю НОВОЙ вкладкой, гоняю `_FIELDS_JS` там. (Записи, что давали «полей: 19», и так открывали detail — поэтому `_COLLECT_JS` предпочитает `discoveryui-content/view`; но строка census часто несёт ТОЛЬКО imageviewer-ссылку.) Скан документа по-прежнему берётся со вьюера (Save-кнопка там работает).
- **Мусор вьюера в полях (`Коллекция|View`, `Zoom in|Zoom out`, `National Archives|Provided in association`)** — ДВА источника: (1) `_docx_add_record` дописывал строку «Коллекция» из `rec["collection"]`, а она = текст ссылки строки результата (`r["coll"]`), у census это **«View»** → фильтровать junk-`coll` (`view/zoom in/zoom out`) и при присвоении, и в Word/Excel; (2) `_FIELDS_JS` ловил chrome вьюера → НЕ извлекать поля, если `page.url` это `/imageviewer/` (карточки там нет), + `BAD`/`BADVAL` режут `collection|zoom|national archives|provided in` (метка) и `view|zoom in|zoom out` (значение).
- **Фолбэк — страница печати**: если полей `<3` и detail-URL не вышел, взять `#printPage` href (`…&pf=true`) → открыть новой вкладкой → тот же `_FIELDS_JS` (печатная версия чистая). Пользователь сама предложила Print.
- **Census — века кликабельны целиком**: заголовки колонок 1700s/1800s/1900s = ЧЕКБОКС-узлы с кодами `cen_century1700/1800/1900` (выбор всего века), раскрываются стрелкой в десятилетия (`_census_columns` строит узел `(век, "cen_century"+век[:4], десятилетия, None)`).
- **Домочадцы** (`household`): таблица с «Household Members/Relationship» (Name|Age|Relationship) → в Word отдельной таблицей «Домочадцы», в Excel колонкой.
- **Подзаголовок** (`subtitle`): «in the 1930 United States Federal Census» = `h1.nextElementSibling`; в Word курсивом под именем, в Excel колонка «Запись».
- Имя из дерева приходит как «Alexander Wolf Sanders\nGoodie Family Tree» → брать ПЕРВУЮ строку (и в сборе, и в `title`).

### Скан документа — кнопка Save (НЕ download-вьюер)
- На странице записи (справа вверху) **`button.save-btn` («Save», `data-tracklink="Save-Open"`)** → выпадашка → **`button.link.item` «Save to your computer»** → `expect_download`. Точную последовательность дала пользователь. Фолбэк — самый большой `<img>` (мелкое превью ~17КБ; настоящий документ только через Save).

### Релевантность / объём
- **Лучше БОЛЬШЕ результатов, чем пропустить релевантное** (правило пользователя). `spouse=` у Ancestry — мягкий фильтр (5503 результата), поэтому в выдаче и «Alexander Sanders» без W — это ОК, не ужимать. Релевантные могут быть и ниже баннера «Don't miss out…» — сбор берёт всю страницу (`count=50`).

### Краулер фильтров (ancestry_filter_crawler.py) — ПОЛНАЯ рекурсия, NO dedup
- **Запускается пользователем из консоли** (`python ancestry_filter_crawler.py`), НЕ из GUI. Шаблон для ещё 3 сайтов (вся специфика в `SITE`). НЕ удалять.
- **ПОЛНАЯ рекурсия по локациям** до последнего уровня (континент→страна→штат→округ→город→…), `max_depth=12` (фактически до листьев), `--depth N` для override. **Даты НЕ краулим** (не нужны).
- **`--us` = ТОЛЬКО США, до 5-го уровня (города)** (по просьбе пользователя): `max_depth=5` + рекурсия идёт ТОЛЬКО в North America → United States, глубже (штаты→округа→города) — всё. Уровни 1-4 и другие страны НЕ трогаются: при resume (дамп есть) 1-4 берутся из дампа, докрауливаются только города под US-округами; `merge_locations` НИКОГДА не удаляет существующее (только добавляет/заполняет коды), так что прочие страны в JSON целы. Города (уровень 5) ложатся рекурсивно в `state.places[county].places[city]` (merge уже умеет любую глубину). Фильтр в `visit`: depth1→только «north america», depth2→только «united states/usa», depth≥3→все.
- **КЛЮЧЕВОЕ ОТКРЫТИЕ: Record Date на Ancestry = `record_f=ДИАПАЗОН`** (тот же параметр, что у локаций!): век `record_f=1500-1599`, десятилетие `1500-1509`, **отдельный год `record_f=1851-1851`**. Поэтому локационный краул ОБЯЗАН исключать диапазоны (`exclude: ^\d{4}-\d{4}`), иначе годы лезут в локации (баг «года ВЕЗДЕ» — было). В скрапере `_pass_url`: код-диапазон `^\d{4}-\d{4}$` → `&record_f=<код>`, а `cen_*` → `/categories/<код>/` (век/десятилетие верифицированы как cen_, годы — как record_f-диапазоны).
- **Места nest РЕКУРСИВНО любой глубины**: `state.places[label]` = строка-код (лист, старые US-города) ИЛИ `{code, places:{…}}` (есть дети). `merge_locations` строит по цепочке предков; GUI `_place_node` рекурсивно рендерит; `_load_filter_codes.walk_places` рекурсивно собирает коды (dict→code+рекурсия, иначе строка). НЕ класть dict как код.

### GUI (как на сайте)
- **Имя/фамилия — МУЛЬТИ-выбор форм совпадения (чекбоксы в дроп-дауне `CheckableComboBox`), ТОЧНО как на сайте**: «**Exact and…**» сверху + ВЛОЖЕННЫЕ (отступ 4 пробела) `Sounds like`/`Similar`/`Initials`, и **некликабельная строка-ссылка «About these settings»** (`add_info` — `it.isCheckable()` False, eventFilter её не трогает, в `checked()` не входит). First: 4 формы (`FIRST_FORMS`), Surname: 3 (`SURNAME_FORMS`). Коды: p=sounds-like, s=similar, i=initials, 1=exact. **name_x — ОДИН параметр** → `_name_x_code` из ОБЪЕДИНЕНИЯ форм first+surname: `fuzzy="".join(p,s,i present)`; fuzzy → он, иначе «1» если Exact, иначе пусто (Broad). `_refresh` показывает метки без отступов (`.strip()`).
- **Мусор формы «предложить альт-имя» лез в таблицу** (`Provide alternate…|Choose…`, `Reason|Choose…`, `Submit alternate|Cancel`, `Apply|Close`, `Add your own|Cancel`, `Attach|Cancel`) → фильтр в `_FIELDS_JS`: BAD += `provide alternate|submit alternate|add your own|attach|reason|apply|edit`, BADVAL += `choose|cancel|close|apply|submit|attach`. Каждая такая пара имеет junk-значение (Choose/Cancel/Close) → BADVAL ловит все.
- **Скролл мыши в дроп-даунах/спинбоксах НИЧЕГО не меняет** (`gui/_app_icon.py::_WheelGuard`, ставится один раз из `app_icon()` который зовут ВСЕ GUI): event-filter на Wheel идёт вверх от виджета — `QAbstractItemView` (открытый попап) → пропустить (список крутится), `QComboBox/QAbstractSpinBox` → `return True` (блок, значение не меняется). Открытие только кликом + прямой выбор. Минус: над комбо панель не прокручивается (приемлемо). **КРИТИЧНО — app-wide фильтр ловит события и НЕ-виджетов (`QWindow` и т.п.), у которых НЕТ `.parentWidget()`** → `AttributeError` спамил лог. Обязателен `isinstance(obj, QWidget)` перед обходом, и `while isinstance(w, QWidget)` (импортировать `QWidget`).
- **FamilySearch — title слипался во ВСЮ панель «person details»**: селектор `[class*="title" i]` хватал контейнер, оборачивающий имя+все поля → `text_content` = «Ruby Theresa ParkerNameRuby…SexFemale10 Oct 1914…» (≈260 симв, <300 → проходил) → это шло в ЗАГОЛОВОК записи. Фикс: title ТОЛЬКО `h1`/`h2`, первая строка, **кап длины <120** (имя не бывает 250 симв → слипшийся блок отсекается, fallback = name_hint). Поля панели (Name/Sex/Birth Date/Race/…) — НЕ dl/table → читать через `_FS_FIELDS_JS` (ровно 2 однострочных текстовых ребёнка, label<45/value<300, junk-метки фильтр), как `_FIELDS_JS` у Ancestry.
- **JewishGen — колонка «Сохранённый файл»** с точным путём скачанного документа: `_process_images_for_db` возвращает `{row_index:[paths]}`, `write_database_docx`/`write_xlsx_all` принимают `row_files` и дописывают колонку (путь(и) через `\n`). JewishGen НЕ вставляет превью (копия таблицы) — поэтому именно КОЛОНКА, не «рядом с превью».
- **«Файл: <полный путь>» рядом с КАЖДЫМ превью документа в Word + колонка «Файл» в Excel — ВО ВСЕХ скраперах** (требование). Путь = `str(Path(saved).resolve())` (директория+имя). Источник пути: `images[0]`/`image_path`/`photo_path`/`scans[]`/`doc_path`/`photo`. JewishGen — ИСКЛЮЧЕНИЕ: он не вставляет превью (копия таблицы результатов + папка с фото по строкам), вставлять некуда.
- **Add event:** строка-ссылки Marriage/Death/Lived In/Any Event → динамические строки (Year + ±диапазон + Location). В URL: `marriage/death/residence` + `<ev>_x=N-0-0` + `<ev>place` (Any Event — параметр неизвестен, пропуск).
- **Add family member:** строка-ссылки Father/Mother/Sibling/Spouse/Child. **Father/Mother — ОДИН** (`_FAM_SINGLE`, ссылка дизейблится после добавления, 2+ нельзя); Sibling/Spouse/Child — неограниченно. Каждая строка: First/Last/Exact/✕.

### Фильтры результатов — раскрывающееся ДЕРЕВО + URL-коды (как на сайте)
Таксономия фильтров — в трёх JSON (единственный источник; редактировать их): **`config/ancestry_record_types.json`** (вложенное дерево категория→подкатегория→коллекции; Census→десятилетия→коллекции; Pictures/Family Trees→коллекции напрямую), **`ancestry_locations.json`** (континент→страна→штат→места, у каждого свой `record_f`-код), **`ancestry_record_dates.json`** (век→десятилетия). Собраны из вордовских экспортов пользователя (`Фильтры*.docx`, `Cantries.docx`, `North America.docx`, `Record Date.docx`).
- **Парсинг ворда**: ссылки несут И код (`/categories/<code>/`, `/collections/<dbid>/`, `record_f=<id>`) И **текст-метку** (текст ссылки подкатегории = ТОЧНОЕ название, брать его — не выдумывать); таблицы между ссылками = коллекции узла. **Порядок: категории/подкатегории/континенты — как в файле (НЕ алфавит); по алфавиту ТОЛЬКО таблицы (коллекции), а также СТРАНЫ и ШТАТЫ** (пользователь так велела). Континенты — фикс. порядок `CONTINENT_ORDER`.
- **GUI = дерево** (`_tree_node`): стрелка ▶ (как тумблер секции фильтров) раскрывает следующий слой, **на поверхности только первый слой**, **дропдаун ТОЛЬКО на листе коллекций** (record type). В **локациях и Record Date дропдаунов НЕТ — только стрелки + чекбоксы** (места/десятилетия = чекбокс-дети, не выпадашка).
- **Census = 3 колонки по векам** (1700s/1800s/1900s), года = **2-й уровень, сдвинуты вправо** (`_census_columns`, decade depth=1, row margin 24). **Record Date = 3 колонки**: 1500+1600 | 1700+1800 | 1900+2000 (`_rd_columns`), века→стрелка→десятилетия-чекбоксы.
- **Ленивая загрузка**: тело узла строится при ПЕРВОМ раскрытии (`_build_body`) — старт окна ~0.03с при 27k+ коллекций (раньше грузил всё → «открывается кошмарно медленно»).
- **Коды в URL**: категория→`/categories/<code>/`; коллекция→`/collections/<dbid>/` (код `collection:<dbid>`); десятилетие→`/categories/cen_1890/`; локация→**`&record_f=<id>`** (НЕ клик — коды есть в нижних гиперссылках ворда). Каждый отмеченный узел/значение = СВОЙ проход/документ; `_pass_url` строит URL, неизвестный код → проход ПРОПУСКАЕТСЯ. `_filter_passes`: record-type+date = категориальные проходы (объединение, не крестятся между собой), локация крестится (AND). `name_x` коды уровней (`_name_x_code`: ps/psi/s/1).
- **Выпадашку коллекций капить `setMaximumWidth(480)`** — иначе при stretch она тянется на всю ширину и стрелка ▾ уезжает ЗА край окна (баг).
- **ГРАБЛИ парсинга (плоский экспорт БЕЗ отступов — иерархию не восстановить точно)**: строки **«Show Fewer»/«Show More»** и **русские заметки** («Азия другой поиск:») просачиваются как «штаты» → фильтровать (кириллица + «Show »). **Страны-как-города/штаты**: `Armenia` попала в `Latvia`, `Albany` в Europe → использовать known-country set + repeat-detection; страны-named-города в drill-down (Lebanon, Mexico) сбивают парсер. Глубокая вложенность county/city — best-effort.
- **Категория 44 = Maps, Atlases & Gazetteers**. **Antarctica** — континент (`_5022`) + French Southern Territories (`_5090`) + Bouvet Island (`_5043`); идёт после Africa.
- Старая клик-схема (`_apply_filter_click`/`_expand_parent`) — ФОЛБЭК для локаций без кода; основной путь — URL-коды. `MAX_PAGES=1` (count=50, пагинация дублировала и сбрасывала клик-фильтры). Каждый фильтр = отдельный документ (`ancestry_{q}__{фильтр}.docx`) + папка фото; конфликт файлов спрашивается.
- Английский, зелёная тема. Креды (username/password+глаз). Basic Search: First/Last/Place/Birth Year, у КАЖДОГО свой чекбокс «Exact» (payload `exact={name,surname,place,year}`). Advanced (сворачиваемый): Family members Father/Mother/Spouse (First/Last + Exact) + Keyword. Конфликт файлов overwrite/append/skip. Файлы `ancestry_{запрос}.docx/.xlsx`, Excel колонка «База»=Ancestry.

---

## Unified Search (unified_search.py + gui/unified_tab.py + вкладка в лончере)

Вкладка «Unified Search» в лончере (gui/launcher.py → `QTabWidget`: «Databases» = карточки, «Unified Search»). Два поля Имя/Фамилия → ищет по ВСЕМ сайтам с персональным поиском, **КРОМЕ Wikisource**. Логин — только MyHeritage (поля в вкладке), остальные ищут без кредов.
- **Перевод = транслитерация** (`translate_name`, Lat↔Cyr, best-effort): сайты с латиницей (Ancestry/FamilySearch/JewishGen) → латиница; кириллицей (Skarb/Память народа/Памяти героев/Memorial) → кириллица; **многоязычные (Yad Vashem, MyHeritage) → имя КАК ВВЕДЕНО** (не переводим). Pogroms = латиница (форма принимает только `[A-Za-z]`).
- **Каждый сайт = `run_scraper(list_only=True)`**: собирает ТОЛЬКО строки страницы результатов (ничего не открывает, не качает, не пишет файлы), возвращает `summary["rows"]` = список dict (колонки страницы результатов). Оркестратор группирует ПО САЙТАМ → ОДИН Word + ОДИН Excel (таблица на сайт, лист на сайт). Ancestry «по ходу задаёт уточняющие вопросы» — игнорируются (URL-поиск их не открывает).
- **Статус: каркас + Ancestry + FamilySearch готовы и сверены** (`list_only` добавлен, early-return после `_collect`, нормальный скрапинг НЕ затронут — флаг default False). Остальные 8 (JewishGen, MyHeritage, Skarb, Pogroms, Memorial, Память народа, Памяти героев, Yad Vashem) — добавить `list_only` в их `run_scraper` (вернуть `rows`) + строку в `SITES`; правка БЕЗОПАСНА (не ломает обычный режим), но колонки строк надо сверять вживую (сайты мне недоступны).

## GUI — общая шапка (gui/_app_icon.py)

- `app_icon()` — иконка окна из `config/app_icon.png` (для всех окон через `setWindowIcon`).
- `make_header(logo_file, title, color)` — лого слева + крупное название по центру (serif 36px). Использовать во всех GUI.
- `make_cancel_button(window, row)` — красная кнопка **Cancel** рядом со Start ВО ВСЕХ скрапер-GUI (требование пользователя). Контракт окна: `_start` ставит `self._cancel_ev = threading.Event()` + `self.cancel_btn.setEnabled(True)`; `_payload` отдаёт `cancel_event=getattr(self,"_cancel_ev",None)`; `_done` — `self.cancel_btn.setEnabled(False)`. Скрапер дёргает `cancel_event.is_set()` и останавливается, сохранив собранное. (Кнопка — единственное добавление в чужие GUI; остальную их логику НЕ трогать.)
- Лончер: лого `config/app_logo.png` (190px) у левого края, двуязычный заголовок по центру между лого и правым краем.
- Логотипы баз: `config/<key>.png` (FSlogo, JGlogo, MHlogo, Skarblogo). Если нет — текстовая заглушка.
- Копирайт везде: `© 2026 Alla Khananashvili`.

### Окно лончера — НЕ открывать «полусвёрнутым»
- Ширину брать с запасом (≥920) под крупный заголовок, иначе текст обрезается.
- `_fit_to_cards()` вызывать **после** показа окна — лучше из `showEvent` (layout «живой» только после показа), иначе:
  - карточки не растягиваются по ширине до ручного ресайза → пустая полоса **справа**. Лечится `setFixedWidth(920)` + форс `centralWidget().resize` + `_outer.activate()`.
  - `sizeHint` слегка завышает высоту → пустая полоса **снизу**. Резать по **реальной геометрии футера**: `self._footer.geometry().bottom() + нижний отступ`, не по `sizeHint`.
- **Открывать ВЫСОКО + капить высоту экраном** (иначе низ окна уезжает за нижнюю границу — пользователь повторяла): в конце `_fit_to_cards` → `final_h = min(final_h, scr.availableGeometry().height()-16)`, затем `self.move(centered_x, scr.y()+8)` (топ почти у верхнего края). `_fit_to_cards` гоняется на нескольких тиках `showEvent`, репозиция на каждом — ОК.
- **Шапка КОМПАКТНАЯ** (лого 110px, титул 25px/15px/11px) — старые 196px+36px не влезали на экраны ниже 4K (пользователь). Ширина окна `min(920, scr.width()-16)`.
- **Карточки — в QScrollArea** (низкое разрешение → окно ужимается до экрана, карточки скроллятся). **`sizeHint` у QScrollArea МАЛЕНЬКИЙ и НЕ связан с контентом** → высоту пинить руками в каждом проходе `_fit_to_cards`: `setMaximumHeight(cards_h+2)` (выше контента нельзя — вернётся «пустая полоса снизу») и `setMinimumHeight(min(cards_h+2, остаток_экрана))`, где остаток = `scr_h-16 − (футер.bottom − высота_скролла)` по ЖИВОЙ геометрии (обвязка шапка+разделитель+футер). На большом экране min=контент → скроллбара нет; на малом — появляется. Тестовый хук `_fit_to_cards(_scr_h=740)` имитирует низкий экран.

---

## Лончер — добавление нового сайта (config/sites.json)

Каждая запись: `key`, `name`, `subtitle`, `action`.
- `"action": "open_url"` + `"url": "..."` — открыть в браузере (нереализованные базы).
- `"action": "open_window"` + `"window_cls": "gui.<module>.<Class>"` — своё GUI-окно.

Добавлено: **Великія описи** (`inv.velikie.org`) — пока `open_url`.

---

## Уроки сессии (фильтр JG, фото pamyat, GUI MH, *.JPG, 503)

### Сессия 2026-06 — ВСЕ мои глупости (gwar + JewishGen) и как чинил (пользователь: «впиши все до последнего слова»)
Сжатый список «что сделал не так → как починил». Подробности — в секциях gwar/JewishGen выше.

**gwar (Памяти героев Великой войны):**
1. **Выдумал URL-ключи `award_type`/`loss_type`** для Наград/Потерь (их НЕТ), хотя пользователь ДАЛА `data-facet-value`. → реальный клик Playwright по `[data-facet-value]` (нормализация пробелов), 5с перед каждым, один «Применить». Почему я тупил: JS `.click()` не тогглит кастомный чекбокс gwar (баг «Выбрано 0/10») — а я не сразу понял, что нужен НАСТОЯЩИЙ мышиный клик, и пытался обойти через несуществующие URL-ключи + спрашивал рабочий URL, который не нужен.
2. **`desc` (Биография) нагрёб ВЕСЬ хром страницы** — добавил «копировать всё с карточки» как leaf-блоки ≥80 симв. с крошечным JUNK-фильтром → в Word попали: «Вы используете слишком старую версию браузера», «Уважаемые пользователи… доступ к личным архивам временно ограничен» (×2), «Новое в 2022-2023…», «В случае обнаружения технических проблем», «Персоналий на образе документа: 0», «Нет данных о точках боевых действий», «Боевой путь строится…», и **полоса номеров страниц «123456…755»**. → расширил JUNK-regex её точными строками + отбрасываю leaf-блок, где цифр >40 и >60% длины.
3. **Выкидывал имя+отчество из URL** (`q.pop("first_name"/"middle_name")`) → поиск ТОЛЬКО по фамилии («Найдено 100 325 Ивановых» вместо «Иванов Григорий»). Вдобавок гонял `_translit` (латиница), хотя живой gwar-URL **кириллический** (`last_name=…=Иванов`). → держать last+first+middle кириллицей как введено; пустое поле — `pop`.
4. **Хардкод `setMinimumWidth(820)`** → на экране ≤820 окно вылезало за край (гориз. обрезка, запрещено). → в `_fit` кап `min(820, экран−16)` + колонки фасетов `colw` под ширину; окно динамически 820↔~1300 при закрытии/открытии фасетов.
5. (раньше в сессии) Клики по фасетам через JS не тогглили → перешёл на URL `famous_type` для «известных личностей». «Найдено=1, ссылок=0» — ждал ТЕКСТ счётчика вместо ссылки; famous-ссылка = `/heroes/commander`, не `/heroes/chelovek` → ждать 5с + поллить ссылку, матчить `heroes-list-item-name`.

**JewishGen:**
6. **Кликал «List N records» по тексту `.first`** → у баз одинаковые счётчики («List 4 records») → коллизия → «Marriages.docx» = Family Finder/JGFF, JGFF открывался дважды. → клик по **idx-й** кнопке (DOM-порядок), пометка `data-jg-click` заново на каждой попытке. + JGFF-футер в `NOISE_RE`.
7. **Первый фильтр не помечен optional** (keyword стал необязательным, а подпись осталась) → плейсхолдер «Keyword 1 (optional)».
8. (раньше) **Семейная группировка по тексту 1-го столбца** склеивала Год/Город/Дату → 79 записей схлопывались в ~1 → откатил, фильтр СТРОГО построчно (rowspan-наследование оставил).
9. (раньше) **Dedup по одному тексту** → 79→50 (одинаковые имена/разный архив схлопывались) → в ключ добавил href’ы.
10. (раньше) **Пагинация исключала кнопки «Page N»** → не шёл на стр.2 (терял 51-79) → кликать `Page {current+1}`.
11. (раньше) **gridifyByTable «Cannot read null (forEach)»** — Playwright дёргал последнюю функцию `_JS_UTILS` → дописал `true;` в конец.
12. (раньше) **Забыл запрос перезаписи файла** → overwrite/append/skip (спросить один раз, переиспользовать).

**MH (код WIP, НЕ коммичу — пользователь сама даст уровни+локаторы):** читал `a[href*=/category-]`, но фасет = `<div class="name">` без href → 0; переспрашивал expand/navigate, хотя структура была дана. Пользователь подтвердила: клик НАВИГИРУЕТ на `category-`/`collection-` (3 уровня, collection=лист). GUI-выравнивание: `addSpacing` вместо одинакового виджета 18×18 → было неровно.

### JewishGen — кнопки «List N records» кликать по ИНДЕКСУ, не по тексту (баг «свадьбы из Family Finder»)
- **БАГ (данные путались между базами):** `click_list_button_open_new_page` кликала кнопку `get_by_role("button", name="List N records").first`. Куча баз имеет ОДИНАКОВЫЙ счётчик (напр. несколько «List 4 records») → `.first` ВСЕГДА открывала ОДНУ И ТУ ЖЕ базу (Family Finder/JGFF). Поэтому в «Marriages.docx» попадал JGFF (исследователи + футер «JGFF FAQ / [English]·[Español] / JGFF Start Page»), а JGFF открывался по многу раз («перед Рождениями и после»).
- **ФИКС:** кликать **idx-ю** кнопку (порядок из `collect_list_button_labels` = DOM-порядок). Помечаю idx-ю `data-jg-click` заново НА КАЖДОЙ попытке (устойчиво к reload) и кликаю её; idx = `i-1` из цикла. Фолбэк на старый матч по тексту, если форма страницы поменялась.
- **JGFF-мусор в `NOISE_RE`** (бэкстоп): `JGFF FAQ`, `JGFF Start Page`, `Frequently Asked Questions`, `Save this as a favorite`, `Another Search`, `[English]·`, `Subscribe to our newsletter`.

### JewishGen — фильтр строго построчный
- **Поиск (search rows) ≠ фильтр (keywords).** Фамилию пользователь ИЩЕТ (уходит в форму JG), фильтр (`f1/f2/f3`) применяется к строкам результатов. Точность поиска решает сайт, не я.
- **Каждая строка результата обязана сама пройти фильтр** (`_JS_EXTRACT`): построчно, БЕЗ «семейной» группировки по тексту первого столбца (она склеивала Год/Город/Дату и тащила чужие записи — был баг с Одессой/Кишинёвом). Группировку оставил только через rowspan: ячейка-`null` (накрыта rowspan) **наследует** значение сверху, чтобы человек под растянутым «Bendery» тоже матчился.
- **OR по умолчанию.** Одно слово (город `Bendery`) → ВСЕ записи города (OR и AND одинаковы при одном слове). Несколько слов: OR = любое, AND = все в ОДНОЙ строке. **НЕ делать AND по умолчанию** — я уже обжёгся: `Bendery`+`Aron` AND дал 7 вместо 27 (пользователь ищет Арона ОТДЕЛЬНО, поэтому Одесса по «Aron» — норма, не баг).

### JewishGen — изображения: **любой `*.JPG` — это ссылка, открывать и сохранять**
- `_IMG_LINKS_JS`: брать `a[href]`, где **href ИЛИ текст** кончается на `.jpg/.jpeg/.png/.gif/.tif` (фото могилы показано как текст-ссылка `IMG_5963.JPG`, файл — в `href`; `a.href` отдаёт абсолютный URL).
- `_do_direct_download`: (1) сам URL картинка → сохранить; (2) иначе **каждую** `*.jpg`-ссылку — отдельным файлом (`dest`, `dest_2`…) через `_save_image_url`; (3) иначе самое большое изображение (`_BEST_IMG_JS`).
- **JOWBR** (кладбище): строка ведёт на `jowbr.php?rec=…`; `_row_image_tasks` ловит `jowbr.php`/`/cemetery/` И любой `*.jpg`-линк → тип `direct`. На карточке лежит `IMG_xxxx.JPG` — он и качается. **Чтобы дойти до фото, строка JOWBR должна сперва пройти фильтр** (город часто только в карточке, не в строке → фильтровать по имени/отчеству, что есть в строке).

### JewishGen — 503 / закрытый браузер (НЕ зависать)
- Закрылся браузер/вкладка («has been closed»/«Target page») → `break` всего прогона, сохранить собранное.
- 3 базы подряд с `TemporaryServerError` → стоп. Ожидания 20/40/60с ×3 (макс ~6 мин, не полчаса). **Антибот не делаю** — только паузы 3–8с; гарантировать «не опознают» нельзя.

### Память народа — фото человека (через браузер, иначе CDN 403)
- CDN `cdn.pamyat-naroda.ru` отдаёт **403** на `page.request.get` ДАЖЕ с referer, а cross-origin `fetch()` режет CORS (проверено).
- **Качать так:** `_img_bytes_via_goto` — навигация отдельной вкладкой `context.new_page().goto(url)` (берёт куки сессии). Если 403 — **фолбэк: скриншот элемента** `a.hero-card-person-photo__item` (его `background-image` уже отрисован, `el.screenshot()` берёт пиксели, сеть не нужна — проверено, ловит CSS-фон).
- **БАГ, который поймал:** `_grab_photos` вызывается РАНЬШЕ `_save_media` → папки ещё нет → `write_bytes` падал `FileNotFoundError` **молча**. Лечится `images_dir.mkdir(...)` в начале `_grab_photos`. Логировать КАЖДЫЙ исход.
- **Ордена:** не привязываться к ≤220px впритык к тексту (на «Сводной» награды — текст). Искать `<img>` по странице рядом с классом/текстом `award/орден/медаль/наград`, исключая портрет (`_photo`/person-photo) и баннеры; качать через `_img_bytes_via_goto`; в Word мелко (`Inches(1.3)`), в папку НЕ писать.
- **Скан в Word — `Inches(3.3)`** (было 5.5, «УМЕНЬШАЙ»). `_SCAN_IMG_JS` — ТОЛЬКО `img[usemap]`; старый «largest image»-фолбэк тащил ПОРТРЕТ как гигантский «документ» (отсюда жалоба про «гигантские ордена»). Имена файлов — по реальному имени (`h1`/строка результата), не «person».

### MyHeritage GUI
- **Год рождения должен УДАЛЯТЬСЯ:** `QSpinBox` не принимает пустой ввод. Подкласс `_YearSpin` (override `validate` → Acceptable для ""/«—», `valueFromText` → 0). На год рождения и смерти.
- **Basic Search** — строки HBox: метка фикс. ширины | поле (stretch) | чекбокс (grid с `setColumnStretch` пускал за границу).
- **«Подвал» при сворачивании Advanced:** `_fit()` мерил СТАРЫЙ `sizeHint` → окно не ужималось, лишняя высота раздувала группы. Лечится: разблокировать высоту, `self._outer.invalidate(); self._outer.activate()`, потом `setFixedHeight(sizeHint().height())`.

### Отладка GUI/документов без живого сайта (КЛЮЧЕВОЕ — не гадать, смотреть)
- GUI **рендерить в PNG**: `QApplication` → окно → `show()` → `processEvents()` → `w.grab().save("x.png")` → прочитать. Так нашёл, что MH-гуйка УЖЕ починена (пользователь смотрела старый скрин).
- `.docx` читать как zip: `word/media/*` + `<wp:extent cx cy>` (914400 EMU = 1″). Так нашёл портрет 5.5″, вшитый как «документ».
- JS внутри строк проверять `node --check` (обернув `const f=(…)`); сетевые гипотезы (403/CORS) — проверять мини-скриптом Playwright, а не гадать.
